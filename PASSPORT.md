# ПАСПОРТ ПРОЕКТА «МИНИ-АПП АВТОЗАПЧАСТИ»

**Дата создания:** 21 августа 2026 
Версия: 1.5.3
Статус: Позиционирование: магазин в TG+web; пакеты R/D/C3 в работе (24.08)
✅ C1 (шаблонизация tenant_config) + C2 (редизайн: 4 таба, липкий поиск, бейджи, аналоги, подбор, VIN, Инфо) — задеплоено 23.08

---

## §0. ЖЁСТКИЕ ПРАВИЛА РАБОТЫ (обязательны к исполнению)

### 0.1 Принцип "Не изобретай велосипед"
**ПРИ СОЗДАНИИ НОВОГО КОНТЕЙНЕРА С АНАЛОГИЧНЫМ СТЕКОМ:**
1. Сначала `docker inspect prostors_app` — извлечь конфигурацию (образ, сеть, env, volumes, command, DNS)
2. Скопировать 1:1, заменить только специфичное (токен бота, имя контейнера, папку кода, БД)
3. Запустить — не должно быть проблем, потому что стек идентичен рабочему
4. **НЕ изобретать свою конфигурацию с нуля** — это нарушение принципа "факты до диагноза"

### 0.2 Правило STOP
- **Любое отклонение от паспорта = СТОП и варианты владельцу ДО выполнения**
- Нельзя "лечить симптомы" без отчёта о фактах
- Нельзя перебирать подходы самостоятельно
- Факты (SELECT/curl/docker inspect) ДО диагноза, не после

### 0.3 Правила работы (из ProStors)
- `.env` править только явной командой; после каждого изменения `grep -c` по файлу; секреты через `read -r K` (в историю не падает)
- `node` на хосте НЕТ — проверка синтаксиса только через `docker run ... --entrypoint node --check`
- Факты до диагноза: если "не работает" — сначала SELECT/curl/docker inspect, потом диагноз
- Длинные вставки — ТОЛЬКО файлом (nbsp/CRLF из чата ломают код), в PuTTY короткие плоские команды
Смягчение (24.08): блоки до 5–7 коротких команд через &&/; — одной вставкой; через ФМ — только файлы.
- После правки статики — bump `?v=` (свои правила кэша — свои строки версий)
- Числовые поля формы → `null`, не `''`
- R24: не обещать несуществующее (в оффере знакомому НЕ обещать до готовности)

### 0.4 Принцип "Дураку полдела не показывай"
Знакомый НЕ знает о проекте, пока не показано готовое демо. Никаких "почти готово", "ещё немного" — только работающий продукт.

### 0.5 Обособленность от ProStors
- Отдельный репозиторий `/root/zapdemo/` (НЕ внутри `/root/prostors-saas`)
- Отдельный контейнер `zap_app`
- Отдельная БД SQLite
- Отдельный бот-токен
- Код/БД/volumes/секреты ProStors НЕ трогать
- Единственные точки контакта: сеть `prostors-saas_prostors_net` (одобрено владельцем) и nginx (server-блок `zap.prostors.ru`)
- **НИКОГДА не смешивать с ProStors
0.6 Правки паспорта
Qwen правит паспорт ТОЛЬКО готовыми командами для PuTTY (патч в контейнере, уникальные маркеры), НЕ текстом для ручного копирования. Владелец выполняет и присылает вывод.**
0.7 Разделение труда
Qwen делает свою работу сам: анализ, правки, подготовка файлов. Перекладывание работы на владельца запрещено. Нарушение = стоп и отчёт куратору.

---

## §1. ОБЩИЕ СВЕДЕНИЯ

### 1.1 Назначение проекта
Telegram Mini App + бот для магазина автозапчастей. Цель: демонстрация знакомому владельца, затем полная передача проекта.

### 1.2 Владелец
- **Имя:** Эрнест Гареев 
- **Telegram:** @ernest_812 ⚠️ **ИСПРАВЛЕНО** (было @777ernest888 — это логин платформы, не username)
- **Telegram User ID:** 2038206387

### 1.3 Обособленность
Проект **полностью изолирован** от ProStors:
- Отдельная папка `/root/zapdemo/`
- Отдельный контейнер `zap_app`
- Отдельная БД SQLite `/root/zapdemo/data/parts.db`
- Единственная точка контакта: сеть `prostors-saas_prostors_net` и общий nginx

---

## §2. ТЕХНИЧЕСКИЙ СТЕК

### 2.1 Backend
- **Язык:** Node.js 22 (Alpine)
- **Фреймворк:** Express 4.21.2
- **Бот:** Telegraf 4.15.0 (long polling) — **КОПИРУЕМ из prostors_app**
- **БД:** SQLite 3.45.1 (better-sqlite3 ^11.10.0)
- **Контейнер:** Docker (образ `zap_app_image`)

### 2.2 Frontend (Mini App)
- **Статика:** Vanilla HTML/CSS/JS
- **Telegram WebApp:** встроен через `telegram-web-app.js`
- **Расположение:** `/root/zapdemo/public/index.html`

### 2.3 Инфраструктура
- **Хостинг:** Beget VPS (45.153.191.22)
- **Web-сервер:** nginx (общий контейнер `prostors_nginx`)
- **SSL:** certbot (общий контейнер `prostors_certbot`)
- **Сеть:** `prostors-saas_prostors_net` (одобрено владельцем)

---

## §3. АРХИТЕКТУРА

### 3.1 Структура проекта
```
/root/zapdemo/
├── data/
│   └── parts.db              # SQLite БД (bind-mount)
├── public/
│   └── index.html            # Mini App фронтенд
├── src/
│   ├── index.js              # Точка входа (Express + Telegraf)
│   ├── routes/
│   │   └── products.js       # API endpoints
│   └── db.js                 # (опционально)
├── .env                       # Переменные окружения (СЕКРЕТЫ ЗДЕСЬ)
├── Dockerfile                 # Сборка образа
├── package.json               # Зависимости
└── smoke.sh                   # Smoke-тесты
```

### 3.2 Контейнер `zap_app`
```bash
docker run -d \
  --name zap_app \
  --network prostors-saas_prostors_net \
  --memory=192m \
  --memory-swap=192m \
  --log-opt max-size=10m \
  --log-opt max-file=2 \
  --restart=unless-stopped \
  --env-file /root/zapdemo/.env \
  -v /root/zapdemo/data:/app/data \
  zap_app_image
```

**Ресурсы:**
- Память: 192MB (hard limit) — OOM убьёт только zap_app, ProStors не заметит
- Swap: 192MB
- Логирование: 10MB × 2 файла (не съест диск)
- Restart policy: `unless-stopped`

### 3.3 Схема взаимодействия
```
─────────────────┐
│  Telegram User  │
└────────────────┘
         │
    ────▼─────┐
    │  Bot API │
    └────┬─────┘
         │
    ┌────▼─────────────────────────────┐
    │  zap.prostors.ru (nginx:443)     │
    └────┬─────────────────────────────┘
         │ proxy_pass http://zap_app:3000
    ┌────▼─────┐
    │ zap_app  │
    │ (Node.js)│
    └────┬─────┘
         ├──→ SQLite (/app/data/parts.db)
         └──→ Telegram Bot (long polling)
```

---

## §4. ФУНКЦИОНАЛ

### 4.1 Telegram Бот
**Реализовано:**
- ✅ Команда `/start` — приветствие + кнопка "Каталог"
- ✅ Long polling (Telegraf 4.15.0)
- ✅ Чистая инициализация `new Telegraf(process.env.BOT_TOKEN)` — **БЕЗ костылей**

**Урок от предыдущего Qwen:** НЕ добавлять `NODE_OPTIONS`, `dns.setDefaultResultOrder`, `https.Agent({family:4})`, `Promise.race` с таймаутами — это костыли, которые не работают. prostors_app работает без них, zap_app тоже должен работать.

**План (Шаг 5):**
- 🔲 VIN-запрос (клиент шлет VIN → TG-пуш владельцу)
- 🔲 Уведомления владельцу о новых заявках

### 4.2 Mini App (Каталог)
**Реализовано:**
- ✅ Поиск по артикулу и названию (debounce 300ms)
- ✅ Отображение товаров (артикул, название, цена, наличие)
- ✅ API endpoints:
  - `GET /api/products?q=&category=&brand=&limit=&offset=`
  - `GET /api/products/categories`
  - `GET /api/products/brands`

**План (Шаг 4):**
- 🔲 Фильтры по категории
- 🔲 Фильтры по бренду
- 🔲 Пагинация
- 🔲 Карточка товара с фото

### 4.3 Админка (Шаг 5)
**План:**
- 🔲 CRUD товаров (добавление/редактирование/удаление)
- 🔲 Импорт Excel одной кнопкой
- 🔲 Список заявок от клиентов
- 🔲 TG-уведомления владельцу

---

## §5. КОНФИГУРАЦИЯ

### 5.1 Переменные окружения (`.env`)
```bash
PORT=3000
BOT_TOKEN=<СКРЫТ>  # ⚠️ ТОЛЬКО в .env, НЕ в документации
ADMIN_PASSWORD=<СКРЫТ>  # ⚠️ Сменить перед передачей (не admin!)
TG_CHAT_ID=2038206387
DB_PATH=/app/data/parts.db
MINI_APP_URL=https://zap.prostors.ru/
```

⚠️ **ПРАВИЛО:** Секреты (BOT_TOKEN, ADMIN_PASSWORD) НИКОГДА не записывать в паспорт, README или документацию. Только в `.env`.

### 5.2 База данных (SQLite)

**Таблица `products`:**
```sql
CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id INTEGER DEFAULT 1,
    article TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    category TEXT,
    brand TEXT,
    price REAL NOT NULL,
    stock INTEGER NOT NULL DEFAULT 0,
    car_brand TEXT,
    photo_url TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_products_article ON products(article);
CREATE TABLE requests (id INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT DEFAULT 'vin', vin TEXT, description TEXT, contact TEXT, status TEXT DEFAULT 'new', created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE tenant_config (id INTEGER PRIMARY KEY CHECK (id=1), brand_name TEXT, shop_name TEXT, slogan TEXT, phone TEXT, contact_info TEXT, about TEXT, payment_text TEXT, delivery_text TEXT, hero_url TEXT, color_primary TEXT, color_accent TEXT, car_brands_json TEXT, categories_json TEXT, tg_chat_id INTEGER, admin_password TEXT, updated_at DATETIME);
```

**Демо-данные:** 10 товаров (4 категории, 4 бренда)

### 5.3 Nginx (требует настройки)

**Требуемый server-блок (только по явной команде владельца):**
```nginx
server {
    listen 443 ssl;
    server_name zap.prostors.ru;
   
    ssl_certificate /etc/letsencrypt/live/zap.prostors.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zap.prostors.ru/privkey.pem;
   
    location / {
        proxy_pass http://zap_app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

⚠️ **ПРАВИЛО:** Nginx server-блок добавлять ТОЛЬКО по явной команде владельца. До этого — не трогать.

---

## §6. ДЕПЛОЙ И ЭКСПЛУАТАЦИЯ

### 6.1 Сборка и запуск
```bash
cd /root/zapdemo

# Сборка образа
docker build -t zap_app_image .

# Пересоздание контейнера
docker stop zap_app && docker rm zap_app
docker run -d --name zap_app \
  --network prostors-saas_prostors_net \
  --memory=192m --memory-swap=192m \
  --log-opt max-size=10m --log-opt max-file=2 \
  --restart=unless-stopped \
  --env-file /root/zapdemo/.env \
  -v /root/zapdemo/data:/app/data \
  zap_app_image

# Проверка
docker logs zap_app --tail 20
```

### 6.2 Smoke-тесты
```bash
# API health
curl http://zap_app:3000/health  # через сеть, НЕ localhost

# API products
curl "http://zap_app:3000/api/products?limit=3"

# Бот
# Отправить /start в Telegram
```

⚠️ **ПРАВИЛО:** Проверки делать через сеть `prostors-saas_prostors_net`, не через `localhost:3000` (порт не проброшен на хост — это правильно).

### 6.3 Бэкап БД
```bash
# Бэкап = копирование файла
cp /root/zapdemo/data/parts.db /root/zapdemo/data/parts.db.backup.$(date +%Y%m%d)

# Восстановление
cp /root/zapdemo/data/parts.db.backup.20260821 /root/zapdemo/data/parts.db
docker restart zap_app
```

### 6.4 Логирование
- Расположение: Docker JSON-логи
- Просмотр: `docker logs zap_app --tail 100`
- Размер: max 20MB (2 файла × 10MB)

---

## §7. КОММЕРЧЕСКАЯ МОДЕЛЬ

### 7.1 Две ветки передачи

**Ветка 1 "Подписка":**
- Разработка: 30–50к ₽
- Поддержка: 3–5к ₽/мес (хостинг + обновления)
- Владелец: знакомый

**Ветка 2 "Передача":**
- Разово: 60–100к ₽
- Передается:
  - GitHub-репозиторий
  - SQLite-файл с данными
  - `.env` (с его доменом и ботом)
  - README (pm2 start, git pull, бэкап)
- Владелец: знакомый (сам хостит)

### 7.2 Расходы клиента (при передаче)
- Домен: ~300 ₽/год
- VPS: 300–400 ₽/мес

### 7.3 Что выбирает знакомый
**После демо** (2–4 недели) знакомый выбирает ветку. Если отказывается — контейнер удаляется по чек-листу.

---

## §8. ТЕКУЩИЙ СТАТУС (на 21.08.2026)

### 8.1 Готовность
- ✅ **Шаг 1:** Inspect prostors_app — выполнено
- ✅ **Шаг 2:** Создание zap_app — выполнено
- ✅ **Шаг 3:** Бот работает (отвечает на `/start`)
- ✅ **Шаг 4 (частично):** API работает, демо-данные загружены
✅ Mini App: zap.prostors.ru — HTTP/2 200 (DNS+SSL+nginx выполнены)
- 🔲 **Шаг 5:** Админка, VIN, импорт — не реализовано

### 8.2 Блокеры
1. **DNS:** A-запись `zap.prostors.ru` → `45.153.191.22` (владелец настраивает в reg.ru)
2. **SSL:** certbot (после DNS)
3. **Nginx:** server-блок (после SSL, только по явной команде)

### 8.3 Что работает
- ✅ Бот: отвечает на `/start`, показывает кнопку "Каталог"
- ✅ API: `/api/products` возвращает 10 товаров
- ✅ Поиск: работает через API
- ✅ БД: SQLite с демо-данными

---

## §9. ПЛАН РАЗВИТИЯ

### 9.1 Немедленно (после DNS)
✅ Владелец: A-запись в reg.ru
✅ Qwen: SSL-сертификат (certbot)
✅ Qwen: server-блок nginx (по явной команде)
✅ Qwen: Mini App протестирован

### 9.2 Шаг 4 (Каталог + фильтры)
1. 🔲 Фильтры по категории (dropdown)
2. 🔲 Фильтры по бренду (dropdown)
3. 🔲 Пагинация (кнопки "Назад"/"Вперед")
4. 🔲 Карточка товара (модальное окно)
5. 🔲 Фото товаров (заглушки или Yandex Object Storage)

### 9.3 Шаг 5 (Админка + VIN)
1. 🔲 Простая админка (HTML-форма)
2. 🔲 CRUD товаров
3. 🔲 Импорт Excel (парсинг XLSX → INSERT INTO)
4. 🔲 VIN-форма (клиент вводит VIN + что нужно)
5. 🔲 TG-пуш владельцу (`https://api.telegram.org/bot.../sendMessage`)

### 9.4 Шаг 6 (Полировка)
1. 🔲 Демо-данные (50–100 товаров)
2. 🔲 UI/UX улучшения (цвета, иконки)
3. 🔲 Обработка ошибок (try-catch, пользовательские сообщения)
4. 🔲 Smoke-тесты автоматические

### 9.5 Шаг 7 (Демо знакомому)
1. 🔲 Показать бота (`/start`, кнопка)
2. 🔲 Показать каталог (поиск, фильтры)
3. 🔲 Показать VIN-запрос
4. 🔲 Показать админку (импорт Excel)
5. 🔲 **Выбор ветки** (подписка/передача)

---

## §10. РИСКИ И ОГРАНИЧЕНИЯ

### 10.1 Технические
- ⚠️ **SQLite:** нет конкурентной записи (до 100 пользователей/день)
- ⚠️ **Telegraf long polling:** может блокироваться (решение: webhook при проблемах)
- ⚠️ **Память 192MB:** OOM-killer при пике (мониторинг `docker stats`)

### 10.2 Коммерческие
- ⚠️ **Знакомый откажется:** удалить контейнер по чек-листу
- ⚠️ **Перегрузка хоста:** zap_app использует ресурсы Beget (мониторинг)

### 10.3 Безопасность
- ⚠️ **ADMIN_PASSWORD:** сменить перед передачей (НЕ admin)
- ⚠️ **TG_CHAT_ID:** проверка прав доступа в админку
- ⚠️ **SQL-инъекции:** параметризованные запросы (уже реализовано)
- ⚠️ **Секреты в .env:** НИКОГДА не коммитить, не записывать в документацию

---

## §11. КОНТРОЛЬНЫЕ ТОЧКИ

### 11.1 Чек-лист перед демо
- [ ] DNS резолвится (`dig +short zap.prostors.ru` → IP)
- [ ] SSL-сертификат действителен (`openssl s_client -connect zap.prostors.ru:443`)
- [ ] Mini App открывается в Telegram
- [ ] Бот отвечает на `/start`
- [ ] Поиск по каталогу работает
- [ ] VIN-форма отправляет уведомление владельцу
- [ ] Админка доступна (пароль НЕ admin)
- [ ] Smoke-тесты проходят
[ ] Демо третьим лицам: `?demo=1` открывается read-only, записи → 403
[ ] Откаты проверены: удалить товар / мусорный импорт → откат → данные целы
[ ] UI-wow ground truth: телефон И ПК

### 11.2 Чек-лист передачи (Ветка 2)
- [ ] GitHub-репозиторий создан (владелец = знакомый)
- [ ] `.env` заполнен (его домен, его бот)
- [ ] README написан (1 страница: установка, деплой, бэкап)
- [ ] SQLite-файл передан (или пустой шаблон)
- [ ] Инструктаж проведен (30 минут Zoom)

### 11.3 Чек-лист удаления (если отказ)
- [ ] `docker stop zap_app && docker rm zap_app`
- [ ] `docker rmi zap_app_image`
- [ ] `rm -rf /root/zapdemo`
- [ ] Удалить A-запись `zap.prostors.ru` в DNS
- [ ] Удалить SSL-сертификат (`certbot delete --cert-name zap.prostors.ru`)
- [ ] Удалить server-блок из nginx

---

## §12. КОНТАКТЫ И ДОКУМЕНТАЦИЯ

### 12.1 Репозиторий
**Путь:** `/root/zapdemo/` (локально на Beget) 
**GitHub:** (создаст владелец при передаче)

### 12.2 Документация
- **Этот паспорт:** `/root/zapdemo/PASSPORT.md` (создать)
- **README:** `/root/zapdemo/README.md` (создать при передаче)

### 12.3 Поддержка
- **Владелец проекта:** Эрнест Гареев (@ernest_812)
- **Техподдержка:** (по договору подписки)

---

## §13. ПРОВЕРКА БЕЗОПАСНОСТИ ДЛЯ ProStors

### 13.1 Изоляция
- ✅ Отдельная папка `/root/zapdemo/`
- ✅ Отдельный контейнер `zap_app`
- ✅ Отдельная БД SQLite
- ✅ Отдельный бот-токен
- ✅ Данные в bind-mount (вне контейнера)

### 13.2 Ресурсы
- ✅ `--memory=192m` (жёсткий лимит) — OOM убьёт только zap_app
- ✅ `--log-opt max-size=10m` (логи не съедят диск)
- ✅ ProStors не использует ресурсы zap_app

### 13.3 Сеть
- ✅ Подключён к `prostors-saas_prostors_net` (одобрено владельцем)
- ✅ НЕ имеет кредов prostors_user/prostors
- ✅ НЕ имеет доступа к API ProStors (там авторизация через initData/PIN/токен)

### 13.4 Nginx/certbot
✅ server-блок zap.prostors.ru добавлен по явной команде (22.08); certbot выполнен

### 13.5 DNS
- ⚠️ A-запись `zap.prostors.ru` ещё не создана
- ✅ Не конфликтует с существующими записями

**ВЕРДИКТ:** Текущая работа безопасна для ProStors при условии завершения изоляции (nginx server-блок только по явной команде).

---

## §14. УРОКИ ОТ ПРЕДЫДУЩЕГО QWEN

### 14.1 Что НЕ работало (костыли)
- ❌ `NODE_OPTIONS=--dns-result-order=ipv4first`
- ❌ `dns.setDefaultResultOrder('ipv4first')`
- ❌ `https.Agent({family:4})` в `telegramOptions`
- ❌ `Promise.race` с таймаутами
- ❌ `disconnect bridge` без одобрения

### 14.2 Что работало
- ✅ Чистая инициализация `new Telegraf(process.env.BOT_TOKEN)`
- ✅ Сеть `prostors-saas_prostors_net`
- ✅ Копирование конфигурации prostors_app

### 14.3 Правило
**prostors_app работает месяцами без костылей → zap_app должен работать без костылей.** Если не работает — СТОП и варианты владельцу, не лечение симптомов.

---

**КОНЕЦ ПАСПОРТА**

*Документ обновлен: 24 августа 2026* 
*Версия: 1.5.3* 
*Статус: Позиционирование: магазин в TG+web; пакеты R/D/C3 в работе (24.08)*
✅ C1 (шаблонизация tenant_config) + C2 (редизайн: 4 таба, липкий поиск, бейджи, аналоги, подбор, VIN, Инфо) — задеплоено 23.08
## §15. УРОКИ ИТЕРАЦИИ 2 (21.08.2026) — v1.2.0

### 15.1 Технические уроки (П1–П9)
- П1: Telegraf работает БЕЗ костылей. Чистая инициализация new Telegraf(token).
- П2/П4: длинные вставки в PuTTY ломаются — только короткие heredoc или файловый менеджер.
- П3: конфиги nginx — ТОЛЬКО на хосте, ФС контейнера read-only.
- П5: docker restart НЕ применяет новый образ — только stop/rm/run.
- П6: nginx кэширует DNS upstream; после рекреации контейнера обязателен reload после зелёного -t.
- П8: docker build/run — ТОЛЬКО с абсолютными путями (/root/zapdemo, --env-file /root/zapdemo/.env). В /root существует чужой .env!
- П8a: fallback пути БД — абсолютный /app/data/parts.db (заменить при следующей правке кода).
- П9: node на хосте НЕТ — синтаксис только в контейнере.

### 15.2 Статус на 21.08
- Бот /start → лендинг с hero.jpg → каталог (10 товаров, поиск).
- https://zap.prostors.ru — HTTP/2 200. ProStors не задет (3 витрины 200).

### 15.3 Далее (Шаг 5 брифа)
- Админка, импорт Excel, VIN-запросы, TG-пуши, кэш-бастинг ?v=.

## 
### 15.4 Дельта шага 5 (закрыт)
- 5.3 Админка: CRUD товаров, заявки+удаление, rate-limit 10/10мин на неверные пароли, вкладки, «Настройки» со сменой телефона/инфо/пароля, кнопки «🔗 Копировать»/«📤 Переслать», «📤 Каталог»
- 5.4 Импорт Excel: xlsx+multer, INSERT/UPDATE по артикулу, отчёт вставлено/обновлено/ошибки, кнопка «Скачать шаблон»
- 5.5 TG-пуш при ошибках импорта
- Бот: `launchWithRetry` скопирован 1:1 из prostors_app (long polling, retry с backoff 5→60 с, `safeStop`, без `process.exit`)
- UI: главная-витрина, hero-картинка формат 4:5, «Начать поиск» в первом экране

### 15.5 Новые уроки
1. Heredoc — только с терминатором + самопроверка `wc -c`/`grep` сразу после записи
2. В HTML — один скрипт-блок либо обязательная проверка баланса `<script>` == `</script>` в каждом деплое
3. Точечный `sed` по повторяющемуся паттерну запрещён (первое совпадение ≠ нужное)
4. Сервер не логирует HTTP-запросы — ground truth только через браузер/curl
5. DoD до работы; одобрение владельца — один раз на пакет
6. UI: не резать картинку — подбирать формат (4:5) и резиновые единицы (vh/vw/clamp)
15.6 Вердикт куратора (23.08):
- Пароли/секреты НЕ в HTTP-заголовках: логин = POST-тело → HMAC-токен в заголовке (паттерн ProStors)
- master-passphrase (unicode в заголовке) = known-broken до Auth-2026
- Пакет Auth-2026 — СТРОГО перед пакетом B (передача); DoD — куратору до работы
- Урок master-401: unicode в заголовке = ломаный класс транспорта
15.7 Уроки C2 (23.08):
- [hidden] перебивается авторским display:flex — всегда [hidden]{display:none!important}
- /api/products возвращает {items:[...]} — проверять форму ответа API ДО написания фронтенда
- не усложнять: несколько правок = один sed -e
- правка фронтенда в терминале — только по прямой команде владельца (иначе FM)
- белый экран телефона: блокирующий внешний скрипт — только defer+guard (паттерн ProStors §15.1 п.57–61 (defer-цепочка + гард))
- веб t.me на телефоне грузится медленно; ссылка корректна (HTTP/2 200) — не код

§1.4 Позиционирование (24.08)
- Продукт = «работающий магазин автозапчастей, живёт в Telegram и в вебе». НЕ продаётся как «мини-приложение».
- Связка TG+web (решение владельца 24.08): TG = вход покупателей + пуши; web = админка на ПК + «существенность» в момент продажи + страховка (t.me вне TG не грузится). В оффере web отдельным каналом не продаём.
- Цена обосновывается бизнес-функцией и временем владельца, не стеком.
- Возражение «в ТГ каждый сделает бесплатно» → «бесплатно — бот с кнопками; я продаю магазин: твой прайс с поиском по артикулу, наличие, VIN-заявки тебе в Telegram, админка. Готово завтра, и если что — звонишь мне».

§0.8 ProStors-first для клиентской инфраструктуры
При любых проблемах с клиентской частью (белый экран, зависание, кэш WebView, SDK, defer, гард, toast):
1. ПЕРВЫМ делом — паспорт ProStors §5 и §15.1 п.57–61/67/74/83–84
2. Копировать решение 1:1, заменить только специфичное
3. ИЗБЕГАТЬ собственных решений для инфраструктуры
Запрещено изобретать: SDK-загрузку, гард diag(), toast-override, defer-цепочки
Разрешено изобретать: только специфику автозапчастей (VIN, аналоги, подбор)

§7.4 Модель передачи (24.08, актуализация §7.1)
- Передача: обфусцированный dist без исходников + лицензия; VPS клиента.
- Обслуживание: 3–5к/мес опционально (добровольная выручка, не подписка); платные доработки = выручка после передачи.
- Граница ответственности (в договоре): код/апп — наша; VPS/домен/бот — клиента.
- НЕ бетонировать как единственную модель вертикали: следующим клиентам — две ветки (подписка на нашем хостинге / передача), по образцу ProStors §8 [3.30].

§2.4 Текущий статус пакетов (24.08)
- R (откаты): cron + бэкапы + админские кнопки откатов — одобрен 24.08, в работе
- D (демо-доступ третьих лиц): `?demo=1` read-only + 403 на записи — одобрен 24.08, ждёт
- C3 (UI-wow, две поверхности: мобайл TG + ПК web) — одобрен 24.08, ждёт
- Порядок: R → D → C3 → заморозка → демо
- Auth-2026 (логин = POST-тело → HMAC-токен, паттерн ProStors; master может остаться русским) — строго перед пакетом B
- VK/MAX — паркинг до первого платящего клиента

§4.4 Новые пакеты функционала (24.08)
- R: cron `0 4 * * *` `sqlite3 .backup` + ротация 7 (backups/ на хосте, в .gitignore); снапшот перед импортом Excel; админка: «Отменить импорт», «Восстановить из бэкапа», «Сбросить настройки»; механика восстановления = копия файла поверх parts.db + `process.exit(0)` (рестарт через unless-stopped)
- D: `admin.html?demo=1` — без пароля, данные видны, записи серверно 403, кнопки disabled + баннер; полный доступ только по паролю; TG-whitelist НЕ строить (бот = покупатели, админка = web)
- C3: UI-wow, две поверхности. Мобайл (TG): 4 нижних таба, липкий поиск, цветные бейджи наличия, skeleton-лоадеры, sticky-низ карточки, «Моя машина» (гараж) + бейджи «Подойдёт», карусель брендов, градиентная шапка. ПК (≥768px): верхняя навигация, 3–4 карточки в ряд, карточка = фото + блок покупки рядом, hero во всю ширину. Всё — CSS-переменные конфига (C1 не ломается). Ground truth — телефон И ПК.

§16. СТРАТЕГИЯ (24.08)
16.1 North star
Channel-agnostic движок (данные одни, поверхностей много) — паттерн ProStors §12.2. Мультиплатформенность = усилитель ценности, не её замена.

16.2 zapdemo как шаблон вертикали
Технически готов (C1 шаблонизация tenant_config). Стратегически — после первого платящего клиента.

16.3 Паркинг: «конвейер вертикалей на общем движке»
НЕ «платформа интернет-магазинов вообще» (красный океан: InSales/AdvantShop/VK Shop). Только после двух платящих кейсов (ProStors + zapdemo). Следующие кандидаты только с доменным экспертом: автосервисы, ветаптеки, стройматериалы.

16.4 Разделение контекстов
В паспорт ProStors вертикаль НЕ вносится (правило разделения контекстов [3.30]).

§15.8 Уроки 24.08 (позиционирование и передача)
- Продаём магазин, не приложение: упаковка «работающий магазин в TG и вебе» сильнее «мини-приложения».
- Web — не аргумент цены; «бесплатно в ТГ» = про примитивный бот; окно бесплатное, магазин — нет.
- Мини-апп нативен в TG, но сервер обязателен: TG не хостит код/БД; «убрать веб» = убрать только вход через браузер.
- Передача на VPS клиента = студийная модель: обслуживание добровольное; без договора с границей ответственности = бесплатная безлимитная поддержка чужого сервера.
15.9 Пакет D (24.08): `admin.html?demo=1` — GET без пароля (заголовок x-demo), записи серверно 403; баннер + disabled; TG-whitelist НЕ строится (бот = покупатели, админка = web).
15.12 Уроки финала 24.08: тост = fixed внизу экрана (msg наверху невидим с низа страницы); после сохранения форма обязана сбрасываться целиком (поля+описание+превью+editId+заголовок); поле, добавленное в админке, = всегда показать в клиенте (description → модалка); жалоба на элемент = аудит всех аналогичных; проверять сценарии (сохранить/редактировать/показать), а не только синтаксис.

## §7.4 Модель передачи
Обфусцированный dist без исходников + лицензия (© Эрнест Гареев; без перепродажи/распространения/реверса под конкурирующий продукт). VPS клиента. Обслуживание 3–5к/мес опционально (добровольная выручка). Платные доработки = выручка после передачи. Граница ответственности (код/апп — наша; VPS/домен/бот — клиента) — в договоре. Следующим клиентам — две ветки (подписка на нашем хостинге / передача), по образцу ProStors §8 [3.30].

## §16 Стратегия
North star = channel-agnostic движок (одни данные + одна админка + много поверхностей, паттерн ProStors §12.2). zapdemo-шаблон технически готов (C1+C2+C3), стратегически — после первого платящего. Паркинг «конвейер вертикалей на общем движке» (НЕ «платформа магазинов вообще» — красный океан) — после двух платящих кейсов (ProStors + zapdemo). Следующие кандидаты только с доменным экспертом: автосервисы, ветаптеки, стройматериалы. В паспорт ProStors вертикаль НЕ вносится (правило [3.30]).

## §15.13 Уроки финала 24.08 (продолжение)
- Лимиты на клиенте И сервере (maxlength + .slice) — страховка от старого кэша
- Retry пуша через 2.5с при transient fetch failed — лечит «[notify] fetch failed»
- edit_hard поверх оригинала — надёжнее обёрток (заполняет ВСЕ поля независимо от исходника)
- collapse_new — форма «Новый товар» сворачивается по умолчанию; при «Ред.» раскрывается
- Пуш: parse_mode HTML + формат «📨 **Запрос #N** / VIN / 📦 товар / 💬 комментарий / 📱 контакт» — без «Что нужно» для товарных (там есть наименование)
- Фото: сжатие на клиенте (canvas 900px JPEG 0.78) до отправки — база не раздувается
- Описание товара в админке = ВСЕГДА показать в клиенте (description → модалка каталога)
- Тост = fixed bottom (msg наверху невидим с низа страницы)
- Жалоба на элемент = аудит всех аналогичных (не точечный фикс)
- Проактивность: если админка «из нулевых» — проверить ВСЕ вкладки, не только ту, на которую ткнули

§17. ДЕЛЬТА 04.09.2026 — ИМ-SaaS v1 (MVP)
Решение владельца (04.09): zapdemo переходит со standalone на SaaS (подписка); знакомый снят с ориентира (не ответил, обязательств нет); ветка «передача» (§7) — архив; zapdemo = демо-тенант (id=1) ИМ-SaaS; модель = подписка, будущим клиентам две ветки по образцу ProStors §8 [3.30]. Паркинг §16.3 пересмотрен: ИМ = первая вертикаль до платящих кейсов, строго обособленно; конвейер следующих ниш — после денег ИМ.
Техника (B1–B5): tenants (id/name/slug/status/plan/expires_at); tenant_config без CHECK(id=1), id=tenant_id; schema.sql идемпотентный (мина рестарта обезврежена); публичное API с ?tenant= (default 1); админка tenant-aware (master = любой тенант, пароль тенанта = свой); витрины index/catalog читают ?tenant= и пробрасывают в API/CTA; admin.html шлёт x-tenant-id; бот /start PAYLOAD → webApp ?tenant=PAYLOAD.
Факты: tenants=1; products(tenant=1)=20; tenant=999 пусто; zap/prostors/app=200; бот /start отвечает (ground truth 21:00). Откаты: *.bak в src/routes и public, parts.db.backup.*.
Урок: «connected to Telegram» в логах может отсутствовать при живом боте — ground truth = ответ /start и API 200.

§18. ДЕЛЬТА 05.09.2026 — боевая платформа (S1–S5)
Приказ (05.09): к 07.09 — полностью боевая ИМ-SaaS; цена 2490/мес, настройка включена; ФЛ до первой реальной оплаты (самозанятость перед первым платежом); outreach 623 — понедельник утро; консультант — офферы/инструкция/матрица тем (спека 05.09, без доступа к серверу).
S1: публичный URL ?tenant=SLUG (ддммгггг-NNN, демо=demo); числовые id в публичном API закрыты; админка резолвит slug; бот /start PAYLOAD=slug.
S2: backups/restore/reset-settings — только master; клиент логинится по слагу и своему паролю.
S3: заявка уходит в tg_chat_id тенанта + платформенный; поле «Telegram для заявок» в админке (chat id через @userinfobot).
S4 приёмка ground truth: пуши в TG; запрос по слагу → tenant_id=2; админка видит только своё; backups → master only; 200/200/200.
Тенант 2 = «Вторая витрина» (второй демо-магазин для продаж).
Финансы: ФЛ — ручные платежи + НДФЛ до самозанятости; эквайринг/рекурренты — после (ЮKassa); приём чужих денег — только ИП/ООО, паркинг v3.

§20. ДЕЛЬТА 05.09.2026 — тарифная сетка + жёсткий тест
Сетка: Старт 2490₽/мес до 3000 товаров (фото 5MB); Бизнес 4990₽ до 15000 (10MB); Профи 9900₽ до 50000 (20MB); Enterprise 19900₽+ индивидуально.
Техника: лимиты в импорте (rows/mb) + upload (photoMb) по plan из tenants; транзакционный импорт (BEGIN/COMMIT/ROLLBACK); unique-индекс (tenant_id, article).
Диск: 8.7GB VPS, 888MB свободно + 509MB build cache (чистится docker builder prune). Запас на 10–15 тенантов до расширения диска.
Фото: сжатие на клиенте (canvas 900px JPEG 0.78), лимиты на сервере — вторая линия защиты.

§21. ДЕЛЬТА 05.09.2026 — полный робот, лендинг v2, уроки
S6 робот: /api/register → tenant+config+slug (slug_seq монотонный), триал 14 дней, пуш владельцу (жирное имя, контакт, слаг), подборщик тем (цвета/слоган/плитки по ключевым словам).
S7 лендинг v2: демо — без плиток, «Смотреть демо-версию…/Создать свой…»; клиент — плитки по теме, без кнопки регистрации; hero ТОЛЬКО из hero_url («Фото на сервере» в админке, без пересборки); градиент бесшовный на всю страницу, оба цвета из настроек.
S7 админка: мастер с ?tenant=SLUG видит данные тенанта; «Откаты» только у мастера; тосты по центру.
Тарифы: Старт 2490 (3000), Бизнес 4990 (15000), Профи 9900 (50000), Enterprise 19900+; лимиты rows/mb, транзакционный импорт.
Инфра: nginx zap-блок client_max_body_size 60m (источник /root/prostors-saas/nginx/conf/zap.prostors.ru.conf), иначе 413 на прайсах >1МБ.
УРОКИ: sed с & ломает код — только python line-replace + node --check ДО деплоя; retry пуша обязателен; мастер-пароль не 'admin'; команды — код-блоками, ссылки — полные.

§23. OUTREACH-КОНТУР 04.09 — yell-парсер + Colab-бот (восстановлено 06.09 по скринам)
Назначение: холодная база продавцов автозапчастей для касаний; 623 контакта на 04.09 (480+143 из двух CSV).
Инструменты: Google Colab на ПК владельца; ноутбуки переименованы: ПАРСИНГ (парсер) и БОТ (outreach-бот, pyTelegramBotAPI). TG-токен бота — ТОЛЬКО в ноутбуке БОТ, в паспорт НЕ вносится.
Парсер yell.ru (бесплатно): телефоны лежат в сыром HTML как schema.org-микроразметка <meta itemprop="telephone">; на списочной странице телефон только в шапке, карточки = ссылки на профили → двухшаговая архитектура: списки (топ + подстраницы метро/районов, MAX_SUB 15–30) → карточки (regex по companies__item-title-text href) → профили (MAX_PROFILES 100–120) → телефон из META_TEL; запасной ANY_TEL отключён после чистки (давал фейки).
norm_phone: только цифры; 8→7; len==11; после +7 только 3/4/8/9; 7800 отсев; is_mobile = +79.
STOP-слова: автосервис, сервисная станция, мойка, сервис, техцентр, автотехцентр (режем сервисы, оставляем магазины).
CITY_SLUGS: 13+ городов со синонимами (moscow, spb, ekaterinburg, kazan, nizhny_novgorod, chelyabinsk, samara, rostov-na-donu, ufa, krasnoyarsk, voronezh, perm, volgograd, krasnodar).
CSV: baza_avtozapchasti.csv / baza_avtozapchasti2.csv; utf-8-sig; колонки: продавец, объявл, телефон, источник=yell, ссылка, город, моб/гор; моб первые.
Антибот yell: после ~1300 запросов с одного IP — блок (первые города успели, поздние нули). Правило: не повторять сразу (IP тёплый) — пауза 6–8 ч, щадящий режим (sleep 2с, меньше подстраниц), новое имя файла.
Бот БОТ («Парсинг запчасти»): принимает CSV → дедуп «название+телефон» → «Загружено новых: N. Всего: M. Жми /next»; /next → карточка (название, телефон, источник, ссылка yell) + «— ТЕКСТ ДЛЯ ОТПРАВКИ —» + кнопки [отправлено|демо|отказ|позже 7|тишина]; /help → статистика (всего/новых/отправлено/демо/отказы/конверсия).
База бота живёт ТОЛЬКО в RAM (base.json при старте не подтягивается) → правило: перезапустил ячейку/вкладку → сразу отправь CSV заново, дубли отсечёт. Бот живёт, пока ячейка крутится [*]; вкладка открыта; ПК не спит (Win7: питание «Никогда» / powercfg -h off).
Текст отправки v2 (актуальный): «Здравствуйте! Нашёл ваш магазин в справочнике автозапчастей. Я разработчик готового интернет-магазина автозапчастей: каталог с поиском по марке и артикулу, корзина, онлайн-оплата, свой домен. Суть: магазин передаётся под ваш бренд — второй канал продаж без комиссий площадок. Могу прислать короткое демо?» (v1 про Авито — снят как нерелевантный).
Каналы: моб → WhatsApp через wa.me/7XXXXXXXXXX (без + и пробелов); гор → звонок, текст голосом. Темп 15–20 касаний/день; мусор → «тишина»; ответил → «демо» → сделка.
Качество базы: компании и ссылки реальные; часть номеров фейк (ANY_TEL) → перед WA сверять номер со страницей yell (10 с); чистка: дубли по номеру + фейк-серии.
Статус 06.09: ноутбуки в Google-аккаунте владельца; CSV в Colab/TG; на сервере таблица outreach (segment test_0609 = номера консультанта под вс-тест; main_0709 = 623 под пн; статусы против двойного удара). Перед пн: открыть Colab → play на ячейке БОТ → отправить CSV заново.

§24. ТЕХСПРАВОЧНИК ТЕКУЩЕГО СОСТОЯНИЯ (06.09) — для восстановления с нуля
Схема parts.db: tenants(id, name, slug, status, plan, expires_at); tenant_config(id=tenant_id: brand_name, shop_name, slogan, phone, contact_info, about, hero_url, color_primary, color_accent, tg_chat_id, admin_password, updated_at — БЕЗ CHECK(id=1)); products(+tenant_id, UNIQUE(tenant_id, article)); requests(+tenant_id); slug_seq(prefix PRIMARY KEY, seq); outreach(id, phone, name, source, segment, status, created_at).
Маршруты: GET /health; GET /api/config?tenant=SLUG; GET /api/products (q/category/brand/limit/offset, только slug, числовые id закрыты); POST /api/register (name, contact с маской телефон/@username, password>=8; slug ддммгггг-NNN через slug_seq монотонно; триал 14 дней; пуш владельцу); POST /api/requests (валидация S15: сообщение>=2, контакт обязателен, VIN 6–17 A-Za-z0-9); /admin/* (заголовки x-admin-pass + x-tenant-id; master = sha256(пароля)==MASTER_HASH, хеш синхронизирован с ADMIN_PASSWORD в .env; rate-limit 10 неверных/10 мин): products CRUD, import-excel (лимиты по plan: rows/mb; транзакционно BEGIN/COMMIT/ROLLBACK), excel-template, upload (фото: canvas 900px JPEG 0.78 на клиенте), settings GET/POST, backups/restore/reset-settings (только master).
Матрица тем (подборщик по ключевым словам названия): oils_filters[масл,фильтр] #1e3a8a/#f59e0b слоган «Всё в наличии»; dismantling[разбор,контракт] #111827/#f97316 «Проверенные контрактные детали»; korean[корей,hyundai,kia] #0f766e/#f59e0b «Специалисты по корейским авто» (плитки в коде = дефолт); default #667eea/#764ba2 «Запчасти с гарантией». Плитки по 4 на тему — пакет 3 консультанта.
Лендинг v2: градиент 180deg primary→accent fixed на всю страницу (бесшовный); слот фото aspect-ratio 4/5 + object-fit:cover; фото = hero_url тенанта, иначе дефолт = фото демо; демо: без плиток, кнопки «Смотреть демо-версию…/Создать свой…»; клиент: плитки по теме, кнопка «Смотреть товары», регистрации нет.
nginx: источник конфига /root/prostors-saas/nginx/conf/zap.prostors.ru.conf (bind-mount в prostors_nginx); в zap-блоке client_max_body_size 60m; правка → docker exec prostors_nginx nginx -t && -s reload.
Деплой: docker build -t zap_app_image /root/zapdemo; stop/rm/run (см. §3.2); docker restart НЕ применяет образ; после правок статики bump ?v=.

§25. АУДИТ 06.09 — недостающее, провалы, правила исправления
В паспорте отсутствовало (восстановлено): весь outreach-контур 04.09 (§23); актуальные схема БД и маршруты (§24); матрица тем; структура лендинга v2; Colab-нюансы (RAM-база, вкладка, sleep ПК); WA/звонок-схема; текст отправки v2.
Провалы Qwen по поиску номеров: (1) не начата разведка живого бесплатного источника и ручная проверка (метод консультанта: десяток номеров за 5 минут) — полдня потрачено на платные/гипотетические базы; (2) зонд сырого HTML (schema.org yell) должен был precede любой код — правило «факты до диагноза» распространяется и на лиды; (3) 04.09 не задокументирован в день работы → потеря контекста, владелец потратил лимит на 65 скринов; (4) вопрос владельцу «формат базы» задан ДО просмотра паспорта/сервера.
Правила исправления (обязательны): R1 — новый инструмент/бот/база = § в паспорте в день создания (где живёт, запуск, файлы, ограничения; секреты — только указанием места). R2 — лиды/номера: сначала ручной зонд 2–3 бесплатных источников + 10 проверенных номеров; ≤30 мин на гипотезу, затем отчёт владельцу. R3 — перед вопросом владельцу: сначала паспорт и сервер. R4 — контекст из любых внешних чатов переносится в паспорт в тот же день.

§26. ОПЕРАЦИИ (06.09) — чек-листы, бэкапы, SSL, секреты, smoke, v2
Утренний чек-лист (2 мин): curl -so /dev/null -w '%{http_code}' https://zap.prostors.ru/health + https://prostors.ru + https://app.prostors.ru = 200; /start боту с телефона = ground truth (в логах «connected» может отсутствовать); docker logs zap_app --tail 30 — нет СЕРИИ «[BOT] launch failed» (одиночный транзиент на старте = норма); ls /root/zapdemo/backups | wc -l = 7.
Важно: с хоста имя zap_app:3000 НЕ резолвится — проверки только через https или docker exec.
Бэкапы: cron 0 4 * * * sqlite3 .backup → /root/zapdemo/backups/parts-YYYYMMDD.db + find -mtime +7 -delete (ротация 7). Ручной снапшот: sqlite3 data/parts.db ".backup backups/parts-$(date +%Y%m%d).db". Восстановление: cp backups/parts-*.db data/parts.db && docker restart zap_app.
SSL zap.prostors.ru: notAfter 18.11.2026; продление — общий контейнер prostors_certbot (зона ProStors, НЕ трогать); контроль 1-го числа: openssl x509 -in /etc/letsencrypt/live/zap.prostors.ru/fullchain.pem -noout -dates.
Если упало: docker ps → нет zap_app → docker logs --tail 50; восстановление ТОЛЬКО stop/rm/run (§3.2); docker restart — только для применения БД/конфигов; серия retry бота = сеть/токен, ground truth /start.
Оплата → активация (ручная до самозанятости): UPDATE tenants SET plan='business', expires_at=datetime('now','+30 days') WHERE slug='…'; + пуш клиенту; чек самозанятого после оформления.
Реестр секретов (значений здесь НЕТ): /root/zapdemo/.env (BOT_TOKEN, ADMIN_PASSWORD=мастер, TG_CHAT_ID, DB_PATH); MASTER_HASH в src/routes/admin.js (синхронизирован с .env); пароли тенантов в tenant_config.admin_password; токен outreach-бота Colab — в ноутбуке БОТ (§23).
Smoke после деплоя: build → stop/rm/run → 3×200 через https → grep-маркеры в HTML → /start и тест-запрос с телефона.
v2-roadmap (НЕ обещать клиентам): оплата картой/ЮKassa — после самозанятости; Авито/маркетплейсы, голосовой поиск, ИИ-генерация — нет; стоки по темам и korean-плитки — v2; webhook — только при нестабильном поллинге; Auth-2026 — строго перед передачей (ветка B).

§28. ПРОДАКШН-ПРАВИЛА И ФОТО (06.09)
R5 (правило владельца): никаких «демо-недоделок» — всё, что делается, сразу продакшн и продаётся; витрина tenant 1 = продакшн-витрина, не черновик.
Фото: на нашей витрине — фото по категориям (8 категорий, файлы data/uploads/stock-*.png; источники: Wikimedia Commons CC-BY + генератор). Графитовая заглушка (SVG-иконка при пустом photo_url) на нашей витрине НЕ используется; это фича v2 для клиентских магазинов: клиент стартует с Excel без фото, каталог не разваливается; фото точечно для дорогих б/у деталей (схема консультанта, одобрена).

§29. ДЕЛЬТА 06.09.2026 — ПОЛНЫЙ ОТЧЁТ ДЛЯ СМЕНЩИКА
(см. отдельный файл PASSPORT_DELTA_0609.md — полная версия)
# ДЕЛЬТА 06.09.2026 — ПОЛНЫЙ ОТЧЁТ ДЛЯ СМЕНЩИКА
Дата: 06.09.2026, вечер
Контекст: владелец принял решение уволить Qwen после серии косяков на витрине и в лидогенерации. Ниже — честный отчёт: что сделано, что не доделано, что делать прямо сейчас.

## 1. СОСТОЯНИЕ СИСТЕМЫ НА МОМЕНТ ПЕРЕДАЧИ

### 1.1 Продакшн-витрина (tenant=demo)
- URL: https://zap.prostors.ru/catalog.html?tenant=demo
- 97 товаров в БД (parts.db, tenant_id=1) из 8 категорий:
  * Фильтры (9) — артикулы Mann/Mahle/Оригинал
  * Моторные масла (6) — Shell/Mobil/Ravenol/Elf
  * Тормозная система (10) — Sangsin/Brembo/TRW/ATE/ТИИР/Алнас
  * Подвеска (18) — Lemforder/CTR/Kayaba/SS20/SKF
  * Электрика (13) — NGK/Bosch/Slon/СОАТЭ/Siemens/КЗАТЭ
  * Охлаждение (7) — ТЗА/SKF/GM/Vernet/ДААЗ/Luzar
  * Трансмиссия (8) — Sachs/LUK/ВИС/SKF/Pascal/Трек
  * Разборка (26) — б/у агрегаты от 1800 до 82000 ₽
- Источник: Excel-прайс консультанта (Demo price-list.xlsx), колонки article/name/price/stock/category/brand/car_brand — 1:1 наш шаблон
- Валидность данных: артикулы реальные, цены рыночные, бренды настоящие
- Лендинг: https://zap.prostors.ru/?tenant=demo — с плитками по теме

### 1.2 Фото на витрине — НЕ ГОТОВО
На сервере лежат 7 файлов в /root/zapdemo/data/uploads/:
- stock-filters1.png (масляный фильтр Wikimedia)
- stock-oil1.png (канистра масла)
- stock-brake1.png (тормозной диск Wikimedia)
- stock-susp1.png (амортизатор Nissan Wikimedia)
- stock-spark1.png (свеча зажигания)
- stock-rad1.png (радиатор Wikimedia)
- stock-clutch1.png (сцепление)
- stock-engine1.png (двигатель)

Применение к товарам — ТОЧЕЧНОЕ через LIKE по названию (sqlite UPDATE), выполнено:
- Фильтры → photo_url=/uploads/stock-filters1.png (всем 9 товарам)
- Масла → stock-oil1.png
- Диск тормозной → stock-brake1.png
- Свеча → stock-spark1.png
- Амортизатор → stock-susp1.png
- сцепл* → stock-clutch1.png
- Двигат* → stock-engine1.png

НЕ ПРИВЯЗАНЫ (photo_url=NULL, чистые карточки):
- Колодки, стойки стабилизатора, рычаги, шаровые, наконечники, подшипники ступицы, катушки, датчики, генераторы, стартеры, помпы, термостаты, радиаторы, вентиляторы, ШРУСы, пыльники, МКПП/АКПП, рейки, компрессоры, фары, бамперы, капоты, двери, зеркала, стеклоподъёмники, панели, замки зажигания
- Это соответствует правилу консультанта: «на старте фото на каждую деталь не нужно», чистые карточки — это продакшн-схема

Генератор-ссылки Qwen-Image не отдаются curl без User-Agent (первые 3 файла с поиска были 478-байтные XML); Wikimedia качается нормально.

### 1.3 Заявки и пуши — ПРОБЛЕМА
- Заявки в БД есть (таблица requests), tenant_id=1
- Запись #27 от 09-05 с tenant_id=-1 — дыра: сервер принял левый тенант, нужно закрыть валидацией (WHERE tenant_id>0)
- В логах 09-05 видно: [notify] fetch failed — транзиент TG API, retry 2.5с не помог (см. §15.13)
- Текущая заявка от покупателя: пуш в tg_chat_id тенанта (1539865812 для tenant=1)
- Скриншот владельца: модалка «Отправить запрос» НЕ имеет маски телефона (+7 (XXX) XXX-XX-XX) и валидации контакта — это мой косяк, не добавил
- Владелец прямо сказал: «никаких тихих сбоев — сбой должен быть виден покупателю, тебе в админке и в базе» — это правило R6 (см. §29.5)

## 2. OUTREACH-КОНТУР — ЧТО ДОГОВОРЕНО С КОНСУЛЬТАНТОМ

### 2.1 Смена парадигмы (согласовано 06.09)
Консультант (оплачивается отдельно за стратегию продаж) предложил архитектуру:
- Источники лидов: VK/TG-парсинг 50%, Яндекс.Карты/2ГИС 30%, Авито-чаты 20%
- Учёт: серверная БД, не RAM Colab
- Сквозная воронка: outreach → tenants → requests одним JOIN

Qwen предложил переход на PostgreSQL/agents/leads — ВЛАДЕЛЕЦ ОТКАЗАЛ: используем SQLite и существующие таблицы tenants/requests.

Финальная согласованная архитектура (взято в работу):
- Бот «Парсинг запчасти» переезжает с Colab НА СЕРВЕР zap_app (Node.js + Telegraf, 24/7)
- Colab убирается полностью
- Таблица outreach в SQLite (уже создана) = единый источник правды
- Поле converted → tenant_id в outreach для сквозной аналитики
- Cron «позже 7» — серверная задача в 10:00 выбирает later_7 старше 7 дней, шлёт в TG
- Токен бота НЕ вставлен — это первый шаг работы (read -r в .env, в историю не падает)

### 2.2 Тексты — в §27 паспорта (сохранены, работают)
- Текст A (первое касание): «Запустим готовый магазин автозапчасти... 2490 ₽/мес... Скиньте прайс»
- Текст B (догрев день 8, статус later_7): напоминание + предложение прислать Excel
- Скрипт звонка (городские номера, до 45 сек)
- 3 микро-ответа на возражения (большой прайс / как выглядит / демо>24ч без файла)

### 2.3 Статусы воронки (для серверного бота)
not_started / sent / silent (прочитано+тишина 3ч) / demo / rejected / later_7 / converted

### 2.4 База лидов на 06.09
- 623 контакта в CSV (baza_avtozapchasti.csv + baza_avtozapchasti2.csv) — парсер yell 04.09
- segment test_0609 — десяток номеров консультанта под вс-тест
- segment main_0709 — 623 под пн
- CSV сейчас в Colab/TG владельца, НЕ в SQLite

### 2.5 Парсер yell (работает, в Colab-ноутбуке ПАРСИНГ)
- schema.org-микроразметка <meta itemprop="telephone">
- Двухшаговая архитектура: списки → карточки → профили
- STOP-слова: автосервис, техцентр, мойка, сервис
- Антибот: блок после ~1300 запросов, пауза 6–8ч
- Всё описано в §23 паспорта

## 3. ПРОВАЛЫ QWEN (для понимания сменщиком, почему уволили)

### 3.1 Поиск лидов — полдня потеряно
Вместо зонда живого бесплатного источника (yell с schema.org) я тратил время на платные базы/export-base/Авито-гипотезы. Консультант за 5 минут ручной проверки нашёл десяток нужных номеров. Правило: сначала ручной зонд 2–3 бесплатных источников + 10 проверенных номеров, ≤30 мин на гипотезу, затем автоматизация.

### 3.2 Документация 04.09
Не задокументировал outreach-контур 04.09 в день работы — владелец потратил лимит на 65 скринов. Восстановлено в §23 только после этого.

### 3.3 Косяки витрины (не закрыты к моменту увольнения)
- Маска телефона в модалке «Отправить запрос» — НЕ добавлена
- Валидация контакта серверная — НЕ проверена, поле пустое проходит
- tenant_id=-1 в заявках — дыра, не закрыта
- Фото применены точечно, но владелец хотел больше соответствия (я применил схему «фото на категорию» которая дала фару с двигателем, потом переделал в точечную LIKE — часть товаров всё ещё с фото-несоответствием)
- Пуш-сбой 09-05 не виден пользователю (тихий сбой) — владелец требует правило R6

### 3.4 Порядок работы — нарушал
- Не всегда делал ground truth перед диагнозом
- Давал длинные команды когда нужно было короткие плоские
- Задавал вопросы до просмотра паспорта/сервера

## 4. ЧТО ДЕЛАТЬ СМЕНЩИКУ ПРЯМО СЕЙЧАС (по приоритету)

### Приоритет 1: закрыть косяки витрины (продакшн!)
1. Маска телефона +7 (XXX) XXX-XX-XX в модалке каталога
2. Серверная валидация: contact не пустой, tenant_id>0
3. Тихий сбой пуша → видимый: push_status в БД (sent/failed), тост клиенту, колонка в админке
4. Проверка соответствия фото товарам — пройтись по каталогу глазами

### Приоритет 2: запустить серверный outreach-бот
1. Получить от владельца токен бота «Парсинг запчасти» (read -r в .env)
2. Написать в zap_app (src/routes/outreach.js):
   - POST /api/outreach/csv — приём CSV-документа, дедуп, INSERT в outreach
   - GET /api/outreach/next?status=not_started&phone_type=mobile — выдача контакта
   - PATCH /api/outreach/:id — обновление статуса с кнопки
   - Cron 10:00 — выборка later_7 старше 7 дней
3. Colab-ноутбук БОТ закрыть, сессия не нужна
4. Защита: мастер-токен в заголовке x-outreach-token

### Приоритет 3: тестовый прогон по 10 номерам (segment test_0609)
- Отправка Текста A в WhatsApp (wa.me/7XXXXXXXXXX)
- Статусы в outreach: sent / demo / rejected / later_7 / silent
- Цель: 3–5 живых ответов, докрутка конверсии перед пн-рассылкой по 623

### Приоритет 4: понедельничная рассылка 623 (main_0709)
- 15–20 касаний/день
- Мобильные — WhatsApp, городские — звонок по скрипту
- Ответ «демо» → ссылка на витрину tenant=demo

## 5. ПРИНЯТЫЕ ПРАВИЛА (обязательны для сменщика)

### R5 (продакшн сразу)
Никаких демо-недоделок. Всё, что делается, сразу продакшн и продаётся. Витрина tenant=demo = продакшн-витрина, не черновик.

### R6 (никаких тихих сбоев)
Любой внешний вызов (пуш, импорт, парсинг) заканчивается видимым статусом:
- Покупателю — тост на витрине
- Владельцу — колонка в админке
- В базе — поле status (sent/failed)

### R2 (из §0.3) — лиды/номера
Сначала ручной зонд 2–3 бесплатных источников + 10 проверенных номеров; ≤30 мин на гипотезу, затем отчёт владельцу. Правило «факты до диагноза» распространяется и на лиды.

### R3 (из §0.3)
Перед вопросом владельцу: сначала паспорт и сервер (SELECT/curl/docker inspect).

### R4 (из §0.3)
Контекст из любых внешних чатов переносится в паспорт в тот же день.

### R1 (из §0.3)
Новый инструмент/бот/база = § в паспорте в день создания.

## 6. СТРУКТУРА БД И ФАЙЛОВОЙ СИСТЕМЫ (справочно)

### Таблицы parts.db (tenant-aware)
- tenants(id, name, slug, status, plan, expires_at)
- tenant_config(id=tenant_id, brand_name, shop_name, slogan, phone, contact_info, about, hero_url, color_primary, color_accent, tg_chat_id, admin_password, updated_at)
- products(+tenant_id, UNIQUE(tenant_id, article))
- requests(+tenant_id) — заявки от покупателей
- outreach(id, phone, name, source, segment, status, created_at) — лиды
- slug_seq(prefix PRIMARY KEY, seq) — монотонные слаги ддммгггг-NNN

### Файлы
- /root/zapdemo/data/parts.db — SQLite БД (bind-mount)
- /root/zapdemo/data/uploads/ — фото товаров (bind-mount, доступны как /uploads/...)
- /root/zapdemo/backups/ — бэкапы БД (cron 0 4 * * *, ротация 7 дней)
- /root/zapdemo/.env — секреты (BOT_TOKEN, ADMIN_PASSWORD=мастер, TG_CHAT_ID, DB_PATH)
- /root/zapdemo/public/ — статика (index.html, catalog.html, admin.html, лендинги)
- /root/zapdemo/src/ — Node.js код (index.js, routes/)

### Контейнер
- zap_app (Node.js 22, Telegraf, Express)
- Сеть: prostors-saas_prostors_net
- Ресурсы: 192MB RAM, unless-stopped
- Деплой: docker build → stop → rm → run (docker restart НЕ применяет образ!)

### Nginx
- Конфиг: /root/prostors-saas/nginx/conf/zap.prostors.ru.conf (bind-mount в prostors_nginx)
- client_max_body_size 60m (иначе 413 на прайсах >1МБ)
- reload: docker exec prostors_nginx nginx -t && -s reload

## 7. КОНТАКТЫ И ВНЕШНИЕ ЛИЦА

### Владелец
Эрнест Гареев, TG @ernest_812, User ID 2038206387
tg_chat_id для пушей владельца: 1539865812 (tenant=demo)

### Консультант по продажам
Работает по стратегии и текстам, НЕ имеет доступа к серверу.
Одобрил:
- Пропорцию источников лидов (VK/TG 50%, карты 30%, Авито 20%)
- Тексты A/B и скрипт звонка
- Серверную схему (SQLite+Telegraf) вместо Colab
- Сквозную воронку outreach → tenants → requests
- Схему «без фото на старте + заглушка + точечно для дорогой разборки»

### Хостинг
Beget VPS 45.153.191.22, 8.7GB диск (888MB свободно)

## 8. ЧТО НЕЛЬЗЯ ДЕЛАТЬ

- НЕ смешивать zap_app с ProStors (отдельный репо, контейнер, БД, токен бота)
- НЕ трогать просторы-контейнеры (prostors_app, prostors_nginx, prostors_certbot)
- НЕ вносить вертикаль в паспорт ProStors (правило [3.30])
- НЕ использовать PostgreSQL (у нас SQLite, миграция = переделка всего)
- НЕ обещать несуществующее (R24)
- НЕ показывать «почти готово» — только работающий продукт (§0.4)
- НЕ вставлять секреты в чат/документацию (только read -r в .env)
- НЕ обещать владельцу эквайринг/ЮKassa/рекурренты до самозанятости
- НЕ обещать Авито/маркетплейсы/голосовой поиск/ИИ-генерацию (нет)

## 9. ПЛАН НА ПОНЕДЕЛЬНИК 08.09

Утро (до 10:00):
1. Открыть Colab на ПК владельца → play на ячейке БОТ → отправить CSV заново (если Colab не убран)
2. Либо: серверный бот уже работает → /next выдаёт контакт из SQLite

День:
1. Рассылка по 623 (main_0709), темп 15–20 касаний/день
2. Ответы продавцов («демо»/«отправлено») — консультанту на докрутку конверсии
3. Сбор возражений → микро-ответы

Вечер:
1. Статистика: всего отправлено, демо, отказы, конверсия
2. План на вторник

## 10. ГДЕ ИСКАТЬ ИНФОРМАЦИЮ

### Паспорт проекта
/root/zapdemo/PASSPORT.md — основной источник правды
§23 — outreach-контур 04.09 (yell+Colab)
§24 — техсправочник (БД, маршруты, матрица тем, лендинг v2)
§25 — аудит 06.09 (что восстановлено, провалы, правила)
§26 — операции (чек-листы, бэкапы, SSL, секреты)
§27 — боекомплект консультанта (тексты A/B, скрипт звонка, микро-ответы, логика воронки)
§28 — продакшн-правила (R5 — продакшн сразу)
Эта дельта — §29

### Чаты
- Консультант: в отдельном чате, без доступа к серверу
- Владелец: TG @ernest_812

### Файлы кода
- src/index.js — точка входа (Express + Telegraf)
- src/routes/products.js — API товаров
- src/routes/admin.js — админка
- src/routes/outreach.js — (НЕ СОЗДАН, это следующий шаг)
- public/catalog.html — витрина покупателя
- public/admin.html — админка
- public/index.html — лендинг

## 11. ОТВЕТСТВЕННОСТЬ

Эта дельта составлена честно: указаны все провалы Qwen, все недоделки, все принятые решения. Сменщик получает полную картину и может продолжать без повторения моих ошибок. Владелец Эрнест прав в своих претензиях: полдня потеряно на лиды, документация не велась вовремя, косяки витрины не закрыты перед продакшн-рассылкой.

Конец дельты.

§30. ДЕЛЬТА 07.09 — ОТКАТ, ИЗВЕСТНЫЕ ДЕФЕКТЫ, ПРАВИЛА К1-К9
Откат 07.09: products.js сломан патчами ui4/p5 (SyntaxError, два красных node --check → К3 запретил третий фикс); last good = 3621 байт из образа zap_app_image (CREATED 06.09 23:27) = файл живого контейнера; откат cp, RC_FINAL=0, деплой по §6.1, smoke зелёный (health=200, аналоги JSON, v16). Спека cross_numbers отложена на 08.09.
Кураторские ошибки (зафиксированы): диагноз «bind-mount src» опровергнут Mounts (bind только data); RC=1 по .bak/.img/.image были артефактом расширения (node --check принимает только .js) → правило К9.
Известные дефекты (статус на 07.09 ночь):
D1 Шапка дрожит при скролле: hdr-hide меняет layout → осцилляция scrollY. В очереди 08.09: transform translateY(-100%) + sticky + rAF + порог 8px, без изменения высоты.
D2 Аналоги невзаимны: условие (X.car_brand=P.car_brand OR P.car_brand='') несимметрично (товар с пустой маркой тянет всех, сам не попадает). В очереди 08.09: симметричное условие + тест взаимности пар.
D3 Фото: категорная схема жива в БД (б/у радиатор с фото двигателя); точечная LIKE-схема — команда в чате 07.09, применить до деплоя 08.09; приёмка глазами по каталогу.
D4 push_status (R6): близнец заявляет деплой до ui4 (БД/тост/бейдж) — проверить ground truth отправкой тест-заявки 08.09.
D5 Рассылка 623 (main_0709) через Colab по §23 — статус подтверждает владелец.
Правила К1-К9 для исполнителя: К1 патч JS = замена целого блока между маркерами; К2 bak до патча, абсолютный путь; К3 два красных check = откат, не фикс; К4 вставка ≤2KB один файл; К5 ground truth curl + приёмка с ?v=; К6 формат факты→диагноз→ОДНА команда→ожидаемый вывод; К7 дата сверена с системой; К8 команда владельцу только после «одобрено» куратором; К9 node --check только по копии с расширением .js.

§32. ИНФРА-КООРДИНАЦИЯ С КУРАТОРОМ ProStors (08.09)
Принят бриф: окно 02:30-03:30 зарезервировано под pg_dump ProStors (1 vCPU общий) — тяжёлые джобы zapdemo вне его; ребилд образа — вечером; vhost zap в общем nginx — только через владельца; /root/prostors-saas, compose и nginx/conf ProStors не трогать; prune только dangling с whitelist (живые образы обоих проектов + zap_app_image:prev).
План infra-вечера: факты (docker history|head, du -sh, docker images|grep zap, docker stats) → tag prev → prune → .dockerignore + multi-stage/slim (цель 150-250MB, причина жира: отдельный слой apk add с компиляторами) → ребилд → recreate с --log-opt max-size=10m,max-file=3 (внести в §3.2) → VACUUM+wal_checkpoint после бэкапа → отчёт владельцу (Up, 200, df -h /, images|grep zap, stats, ls backups|wc -l).
Память: лимит §3.2 = 192m; цифра 221MB из брифа противоречит лимиту — проверить docker stats, предохранитель 350m только после замера пика на импорте.
Offsite бэкапов: вариант б (бот шлёт parts-*.db документом в TG раз в неделю) — ждёт решения владельца.

§33. РЕШЕНИЯ КООРДИНАЦИИ 09.09 (кураторы zapdemo+ProStors)
Offsite бэкапов: вариант (б) с шифрованием — BACKUP_KEY=openssl rand -hex 32 строкой в .env (не передаётся); openssl enc -aes-256-cbc -pbkdf2 -iter 200000 -salt -pass pass:$BACKUP_KEY; бот шлёт только .enc; до готовности крона — режим (а): владелец качает parts-*.db из админки раз в неделю. Основание: ПД покупателей, 152-ФЗ.
Память: 221MB из брифа = артефакт чтения stats (процент от лимита); факт ≈44MB; лимит 192m оставлен; предохранитель 350m отложен до замера пика на первом крупном импорте.
Диск 91.2%: prune до ребилда бесполезен (reclaimable 0B); освобождение = после ребилда multi-stage, когда 732MB станут dangling; тег zap_app_image:prev снимается и prune делается НА СЛЕДУЮЩЕЕ УТРО после приёмки новой сборки владельцем.
Регламент вечера: куратор zapdemo сообщает старт работ первой строкой; отчёт по п.8 (Up, 200, df -h /, images|grep zap, docker stats --no-stream zap_app, ls backups|wc -l) присылается ДО шага B куратора ProStors (nginx+лог-ротация); окно 02:30-03:30 — только pg_dump ProStors.
§34. ДЕЛЬТА 10–11.09.2026 — витрина продакшн + серверная валидация заявок (v18→v36)
Контекст: день начался с приёмки deep-link карточки, закончился зелёной матрицей обеих форм заявок на двух поверхностях; ProStors не задет за весь день (K22 в каждой цепи).
34.1 Клиент (public/catalog.html, v18→v36)
- C4: фото-блок карточки (m_photo), чипы (m_chips), collapse описания; аналоги кликабельны всей карточкой с вспышкой; deep-link ?p=ID (param-код) + hash-fallback.
- Тост: по центру экрана, тёмный #333 (решение владельца 10.09, перебивает §15.12 bottom); тост на каждой ветке сбоя (R6); зонд ?p=999999.
- Свайп-гард: закрытие только свайпом вне скролл-зон (.analogs-scroll,.desc-content) и при scrollTop<=2; скролл контента не закрывает модалку.
- Крестик ✕ справа сверху (m_close, closeModal).
- Форма заявки карточки: форма под кнопкой (openAsk: scrollIntoView center+flash+focus), askBusy «Отправка…»+disabled на время запроса, сброс после успеха.
- Телефон: делегированная маска уровня document (maskPhone v2): 8→7, авто-добор 7 при отсутствии кода, «+7 (» с первой цифрой (без фокус-пресета), ровно 11 цифр (10 после +7), канон +7 (XXX) XXX-XX-XX; латиница и @ в поле телефона не принимаются.
- Вариант B (решение владельца 11.09): контакт = только телефон; Telegram = отдельное необязательное поле a_tg/v_tg, пресет «@» (focusin), голый «@» = не указан, авто-транслит русской раскладки в латиницу, валидация @username 3–32.
- Унификация (решение владельца 11.09, эталон = форма карточки): поля VIN-формы класс .fin (тот же визуал); sendVin полностью на тостах (inline vin_msg убран), сброс полей включая TG.
- sendVin/sendAsk: прямой document.getElementById с null-гардами (без помощника $), try/catch: покупателю русский тост, сырая ошибка только в console.error; «⏳ Отправляю...» на входе.
- fetchWithRetry: 2 повтора @2.5с на /api/products + тост при финальном сбое (тихой пустой каталог исключён).
- initContacts: отложенная навешка масок (DOMContentLoaded/немедленно), null-safe.
34.2 Сервер (src/routes/requests.js)
- Tenant: только известный slug или id>0, иначе 404 «магазин не найден» (дыра #27 tenant_id=-1 закрыта гейтом nn>0; запись #27 от 05.09 историческая).
- Contact: только телефон, ровно 11 цифр (7/8+10), канонизация в +7 (XXX) XXX-XX-XX до INSERT и пуша; VIN 6–17 A-Za-z0-9; description >=2 символов.
- TG-маркер '\n\u2709\uFE0F TG: @name' в description: сервер парсит, валидирует @username 3–32, вырезает из описания, пушит отдельной строкой ✉️ (не режется лимитом комментария).
- Пуш: 📱 канонический телефон + ✉️ TG. Ground truth: пуши #29–#45 в TG владельца (D4 закрыт).
<!-- DELTA34P1 -->
34.3 Правила дня (в правила исполнителя, K16–K28)
K16 guard+retry на DOM/API-вызовах; K17 `!` в командах владельцу только в одинарных кавычках (history expansion); K18 никаких догадок id/строк — позиционный поиск или факт grep, при несовпадении точной строки замена по индексу строки/regex без кавычек; K19 backslash в замене re.subn только через lambda; K20 проверки grep, где ноль=успех, не цеплять через &&; K21 node --check извлечённого inline-скрипта в деплой-цепи ДО деплоя; K22 ProStors-блок (prostors=200, app=200, контейнеры prostors Up) в каждой деплой-цепи и отчёте; K23 при клиентской ошибке сначала консоль ПК F12, потом телефон; K24 верхнеуровневые DOM-ссылки только отложенные и null-safe; K25 assert по счётчику учитывает все формы маршрута, замены scope-ить по функции; K26 делегированные listener'ы полей вместо точечных; K27 emoji в коде только \u-эскейпами (paste-safe); K28 правки форм только через полную матрицу (обе формы × все ветки) одним прогоном.
34.4 Провалы дня (честно)
- ~1 час пустой витрины (v25/v26): верхнеуровневый вызов smartContact с null убивал весь скрипт; диагноз «транзиент» поставлен без проверки второй поверхности — нарушение; лечение: отложенный initContacts + K23/K24.
- Серия красных патчей на точных строках (глифы кавычек, байты emoji, коллизия якоря var b={vin: в sendAsk и sendVin) → K18/K19/K25/K27.
- Paste >2KB один раз рассыпался (bash-flood) → протокол: один блок на сообщение, heredoc ≤~2.5KB, вставка по одной.
- Откат объявлен зелёным без вывода (фактически не выполнялся) → статусы только по выводу команд.
- Решение владельца 11.09: «хватит слать планы, работай» — минимум prose, один блок за раз.
34.5 Статус на конец дня и очередь
Витрина v36 зелёная на телефоне и ПК; матрица заявок зелёная (карточка+VIN: короткий номер→русский тост, полный→пуш 📱+✉️, TG-транслит, крестик, свайп-гард, тост-центр); зонды short=400, badtenant=404; K22 зелёный весь день.
Очередь: C5 (стоковые фото при рождении карточки + исключение б/у + плашки в списке + Wikimedia-батч); рассылка 623 (§23, Colab, понедельник); вечерняя инфра §32/§33 (отчёт куратору ProStors по п.8 до его шага B); правка §24 S15 паспорта (contact = только телефон + TG-поле, вариант B).
Бэкапы дня: public/catalog.html.bak-* (r8/sw/ci/ci2/ask/c6/c6b/c7/c8/b/b2/b3/uni/uni2/w/w2), src/routes/requests.js.bak-dg-0911, src/routes/requests.js.bak-tg-0911, PASSPORT.md.bak-34-0911.
<!-- DELTA34P2 -->
§35. ДЕЛЬТА 11.09 (продолжение) — директивы владельца + статус C5
Директивы владельца (11.09, обязательны):
- K30: ВСЕГДА слать владельцу полные ссылки в каждом сообщении (админка, каталог, VIN, зонды, приёмка); владелец ссылки в памяти не держит. Реестр ссылок: админка https://zap.prostors.ru/admin.html (тенант: ?tenant=demo), каталог https://zap.prostors.ru/catalog.html?tenant=demo, лендинг https://zap.prostors.ru/?tenant=demo, health https://zap.prostors.ru/health.
- K29: в любой ситуации сначала смотреть, что говорит паспорт; ситуация не отражена — после завершения занести в паспорт дельтой командой (§0.6).
- Проактивность строго по паспорту; K22 (проверка угрозы ProStors: prostors=200, app=200, контейнеры prostors Up) — регулярно, в каждой цепи и отчёте.
C5 статус (11.09): БД: 6 б/у очищены от стоковых фото, 37 не-б/у получили сток по категории, 26 остались без фото (только Разборка, чистые карточки); снапшот backups/parts-YYYYMMDD-c5.db. admin.js: gphoto(name,cat) — карта категория→stock, Разборка→null (исключение б/у), вызовы в INSERT/UPDATE с b.category (деплой следом). Клиент: плашка в списке уже живая (photoHtml: плитка emo(category)), клиент C5 не тронут. Wikimedia-батч: после зонда URL (R2).
<!-- DELTA35 -->
§36. СТРАТЕГИЯ 11.09 (решение владельца) — platform-first, аксиома снята
- Аксиома «не развиваемся до первого платящего клиента» (§16.2/§16.3) СНЯТА. Развитие не привязано к платящим клиентам и к знакомому (знакомый снят с ориентира окончательно: не отреагировал на предложение).
- North star: лучшая платформа для размещения интернет-магазинов. Клиенты придут к действительно лучшей платформе, а не до неё.
- zapdemo (магазин автозапчастей) = флагманский шаблон и витрина платформы: он допиливается, чтобы показывать потенциальным клиентам, как будет выглядеть их собственный магазин. Любое его улучшение = улучшение платформы (мульти-тенантно, шаблонизируемо).
- Идеи близнеца платформенного уровня (ИИ как встроенная фича платформы, разведка конкурентов по ИИ в e-commerce, позиционирование «ИИ работает на вашем портфеле») — в области рассмотрения, НЕ в паркинге «до денег».
- Правило исполнителя K32: проактивен ко всем вариантам развития, советам и предложениям; никакой стены «а у нас не так»; паспорт фиксирует решения владельца, владелец меняет курс в любой момент — исполнитель перестраивается в тот же день и заносит дельту.
- §16.2/§16.3 читать в новой редакции: шаблонизация (C1) = ядро платформы; «конвейер вертикалей» = путь роста платформы, а не паркинг до денег.
<!-- DELTA36 -->
§37. ДЕЛЬТА 11.09 (вечер) — порог предложения, близнец №2, решения
Порог «не стыдно предлагать клиентам» (критерии, все проверены): (1) E2E-цепь нового клиента зелёная: регистрация S6 → слаг → тема → импорт реального прайса 100-500 строк → товары на витрине → заявка покупателя → пуш в TG тенанта → админка тенанта видит только своё → откат → ?demo=1 read-only; прогон телефон+ПК; (2) админка ежедневная: списки-пресеты (A6B4), робот «Создать из текста», A7 (табы Товары/Заявки + гамбургер, поле «Состояние», бейдж в списке); (3) упаковка не врёт: тарифная с честным блоком «чего пока нет», docs quickstart + GIF «прайс→магазин», оферта/политика/реквизиты/телефон, статус-страница, кейс zapdemo «собрано за один вечер, вот шаги»; (4) устойчивость: restore-check бэкапа, nginx fallback-снапшот демо, метрика активации, стоп-лист скрипта; (5) K22 зелёный. Оценка расстояния на 11.09: 2-3 дня плотной работы.
Метрика победы (близнец №2, принята): первая VIN-заявка или заказ тенанта в течение 14 дней после импорта; события активации import_done/link_sent логируются с первого звонка; «магазин создан» = vanity.
Критерий разворота (принят ДО боли): 100 звонков → меньше 3 платящих → меняем оффер/ICP, а не код.
Деньги: самозанятость = шаг владельца на этой неделе (оформляется за день); оплата нашей подписки = ЮKassa после самозанятости (§18 уточнена: подписка = услуга, блокер меньше, чем казался); эквайринг тенанта = свой у тенанта, наша интеграция = v2, до готовности в стоп-листе. Чтение R24: онлайн-оплату в звонках не обещаем до живой ЮKassa.
Рамка скрипта (близнец №2): главный конкурент = статус-кво (Excel+WhatsApp+Авито, боли не чувствуют); продаём «что изменится в моих деньгах», не «платформу»; второй конкурент = франшизы («зачем свой магазин, если Экзист даёт поток»).
P5 supplier-фиды (XML/CSV авто-обновление цен/остатков поставщиков) = ретеншн-ров, ПОСЛЕ порога; спека = разбор форматов от близнеца; data-ров легально: кросс-референсы/OEM из прайсов поставщиков и тенантов с согласия тенантов; TecDoc НЕ трогать (лицензия съест бизнес).
Сбор данных начат решением 11.09: price_points(tenant,article,price,stock,ts) триггером на импорт/правку цены + search_log(q,tenant,ts) из /api/products; индекс аналогов = потом из истории.
Не распыляться (§36): другие ниши и партнёрские каналы/SEO/тарификация от ценности/первый найм — после ~30 платящих в авто и порога MRR.
Техстатус вечера 11.09: B-миграция применена (condition; 26 б/у раскладаны по Двигатели/Кузов/Трансмиссия/Электрика/Охлаждение/Подвеска; Разборка исчезла; демо-товары: 3 аккумулятора, 2 колёсных); admin.js: condition в CRUD, gphoto(name,cat,cond) с исключением б/у, генерация ART- при пустом артикуле; catalog v37: бейдж «Б/у» в карточке и модалке; admin.html: A6/A6B1/A6B3 (кнопка «Создать новый товар», артикул необязательный, селекты Категория/Бренд/Марка с «➕ Создать новый…», обёртки saveProd/editProd с тостом сбоя); в очереди: A6B4 (самозагрузка словарей), робот «Создать из текста», A7, N1, упаковка, устойчивость.
<!-- DELTA37 -->
§38. ДЕЛЬТА 13–14.09.2026 — уборка, smoke, волна, бот, предел парсера
K35-уборка 13.09: dated-цепи .bak и patch_*/chk_* удалены (архивы /root/rollback-20260913.tgz и rollback-20260913b.tgz вне проекта); rolling-копия PASSPORT.md.bak; диск 94%→81% (docker prune 1.47GB). K35: патч-скрипты только /tmp, одна rolling-копия, chk удаляются сразу, архив rollback один вне проекта.
smoke.sh zapdemo: df-порог 90% + curl -m 10 (health/prostors/app/ernest); smoke обеих платформ зелёные (K22).
Outreach: таблица outreach создана в parts.db 13.09; волна wave1 = 30 контактов консультанта (source='consultant', campaign='wave1', segment spb/region, дедуп, инвариант added+dup_list+dup_db=30).
Бот: src/bot.js на своём токене (.env BOT_TOKEN, бот @zapdemo777bot): гейт OWNER_TG_ID, /next /nextt /status, приём документа, retry 5s, лог TG UPDATE; legacy-поллер в index.js погашен (один токен = один поллер); старый бот владельца @parsing_zapchasti_bot не тронут; бот-ссылка клиенту не шлётся никогда.
Парсер v1.4 предел (14.09): словесные цены типа «три тысячи четыресто» не распознавались (цена пустая, имя не вычищено) — фикс словаря чисел в работе; пост и офферы не обещают этот кейс до зелёной приёмки (R24).
Вертикали: репетиторство/фитнес/LMS — стоп-лист (нет тонущего контента, ядро операционки вне платформы); автоподборщики = вертикаль №2; S-серия self-serve (signup→поддомен→превью→модерация одним кликом) в очереди после A7.
§39. ДЕЛЬТА 14.09.2026 — парсер v1.5 (словесные числа), проверка поста, вертикали, K36
Парсер v1.5 (admin.html, патчи A/B/C 14.09): wnum() = составные словесные числа (единицы/десятки/сотни/тысячи/миллионы + разговорное «три четыреста»=3400); цена из словесной последовательности с маркером руб/₽ или тысяч/сотен, границы слов lookaround; наличие из словесных форм («в наличии две»→2, «в наличии» без числа→1); чистка имени: вычитание ценовой фразы vm[0], слов штуки/руб/рублей, пунктуационных токенов и краёв, капитализация первой буквы; описание перегенерируется из чистого имени. Приёмка зелёная: тест1 «фильтр масляный для камри в наличии две штуки три тысячи четыреста рублей» → имя «Фильтр масляный для камри», цена 3400, наличие 2, Фильтры, Камри; тест2 «стекло лобовое для Lamborghini, Италия, цена 35000, в наличии» → имя «Стекло лобовое для Lamborghini», 35000, 1, Кузов, Lamborghini, Италия. R24 для этих фраз в посте снят.
Уроки патчей: JS \w без флага u = только ASCII (руб\w* резалось на «руб», хвост «лей») — для кириллицы [а-яё]*; якоря python: raw-строки, двойные бэкслеши ТОЛЬКО внутри JS-RegExp-строк, одинарные в regex-литералах; якорь-строка целиком = позиционная замена по startswith (ноль экранирования); цепь патча = cat > /tmp/pc.py + && (падение патча останавливает деплой).
Пост-чек 14.09: сняты завышения («за один вечер/3 дня» → честные сроки, «пара кликов» → «за один день из прайса» до зелёной S-серии, «применимость» → «для какой машины», «без ручного заполнения» → предзаполнение + подтверждение владельцем, уникальность → личная эмоция); ссылка в посте = https://zap.prostors.ru (витрина); ссылка на бота клиенту не шлётся никогда; CTA = прайс в ответ → бесплатное демо за день. Картинка: генератор ломает кириллицу → финал без текста, надписи поверх вручную или коллаж скриншотов «диктую→карточка→витрина».
Вертикали 14.09: репетиторство/ЕГЭ, фитнес-инструкторы, LMS — стоп-лист (нет тонущего контента либо ядро операционки запись/оплата/прогресс вне платформы; рынок занят YClients/GetCourse/Tilda/Profi); автоподборщики = вертикаль №2 (тонущие кейсы, высокий чек); операционка запись/оплата переиспользуема в 7+ нишах, но её разработка = feature-creep до первых платящих.
Бот: жизненный цикл исправлен (retry 5s на BOT ERR, graceful stop SIGTERM/SIGINT, сквозной лог TG UPDATE, dropPendingUpdates на первом старте); приёмка пунктов 1–4 на @zapdemo777bot (свой /start+/nextt, чужой → «нет доступа») — ожидает теста владельца.
K36: дельта дня дописывается в паспорт командой до конца сессии; строка «дельта в паспорте» обязательна в отчёте исполнителя.
Очередь: A7 → S1–S5 (self-serve signup) → N1 → упаковка/лендинг с живым демо → P2b.
§39.1 (14.09, ночь): приёмка бота завершена: свой аккаунт /start → подсказка команд, /nextt → карточка wave1 (VAG-Бункер, текст А), чужой аккаунт → «нет доступа» (негатив-тест OK, гейт OWNER_TG_ID доказан); волна wave1 (30 контактов) в боевой эксплуатации: 5 тестовых отправок текста А (3–4 СПб + 1–2 региона), статусы через /status <телефон> silent|demo|reply, возражения → консультанту на докрутку.
§40. ДЕЛЬТА 15.09.2026 — бот-пульт v2 (самодокументируемый), директивы владельца
Директивы владельца (14–15.09): бот = полевой пульт outreach и обязан описывать себя сам (владелец не держит команды в памяти и не ходит на сервер за прозрачностью); /stats без арифметики в голове владельца.
Бот v2 (src/bot.js, патчи A/B 15.09, деплой зелёный): SDESC-карта meanings всех 14 статусов; /start = справка (все команды: /next /nextt /status /stats /calls /who + статусы с расшифровкой); /status при ошибке печатает формат и статусы с meanings; /stats = самодостаточный отчёт (всего в базе, по кампаниям, «Обработано X из N», очередь/на руках/обзвон, касания/ждут ответа/тёплые/исходы/bad, конверсия = победы/(касания−bad), построчно каждый статус со счётом и meaning); /who <статус> = список до 20 контактов (имя+телефон) из чата.
Волна wave1: в боевой эксплуатации; поток владельца: /nextt → sent_wa/sent_tg/called; нет в мессенджерах → needcall (очередь /calls); подтверждённо мёртв → bad; следующий шаг бота (B-серия): крон 10:00 догрева silent/later_7 старше 7 дней текстом B.
§41. ДЕЛЬТА 17.09.2026 — парсер v1.6 (марка/модель), поле model, no-store, backups-монт, диск 91%
Директива владельца (17.09): робот понимает любую модель: «камри»→Toyota+Camry, «витара»→Suzuki Vitara; в названии каноническое написание; поле модели в админке подписано и стоит между «Марка авто» и «Новая марка».
Парсер v1.6 (admin.html): MODEL_MAP 67 моделей/134 написания + BA ~38 марок; алиас модели → car_brand=марка+model=модель (канон); только марка → канон; неизвестно → фолбэк «для X» (R24, без выдумывания); цена извлекается ДО наличия (любой порядок слов); vm-цикл по всем словесным последовательностям (первая с маркером руб/тысяч/сотен); «четыресто»=400 в словаре чисел; словесные границы в regex наличия (префикс «четыре» внутри «четыресто» не матчится); алиас в названии заменяется канонической моделью («Фильтр масляный для Camry»).
Схема/CRUD: products.model пишется в INSERT/UPDATE admin.js (колонка существовала, но всегда NULL); форма: input «Модель авто» с подписью между маркой и «Новая марка»; editProd/aiFill/payload/сбросы включают model; genDesc «Применяемость: Toyota Camry».
Данные: разовый UPDATE car_brand='Камри' → Toyota/Camry (демо).
Инфра: index.js Cache-Control no-store для .html (класс «старая вкладка»); монтирование /root/zapdemo/backups:/app/backups:ro (лечит 500-флуд /admin/backups, оживило «Откаты»); nginx reload после рекреации обязателен в деплой-цепи (П6).
Уроки цепи: индекс grep = номер строки − 1; якорь = префикс строки/уникальная подстрока с assert; клиентская приёмка только после серверного ground truth (node eval aiParse из образа) и новой вкладки; статусы только по выводу команд.
Диск 91% (DISK_CRITICAL): трек 17–18.09 — факты (docker system df, du buildkit, images), docker builder prune + image prune dangling (whitelist §32), тег zap_app_image:prev снять + финальный prune утром 18.09 по §33 (приёмка новой сборки состоялась 17.09); цель DISK_OK (<90%).
Приёмка зелёная: node eval 3/3 (Toyota/Camry/3400/2 при обоих порядках слов, Lamborghini/35000/1) + новая вкладка ПК: название «Фильтр масляный для Camry», модель Camry в подписанном поле; smoke 4×200 (K22).
§41.1 (17.09, закрытие): K35-поправка — PASSPORT.md.bak удалён, страховка паспорта = git-коммит после дельты (репо /root/zapdemo, локальное); трек диска закрыт: builder prune 619MB, df 85%, DISK_OK, smoke 4×200 (K22); bak-файлов в проекте ноль. Следующая сборка без build-кэша (несколько минут) — норма.
§41.2 ДЕЛЬТА 17.09.2026 (вечер) — /links, human-health, стем-схема, аналоги по применимости (вариант A), prune-правило
Бот: /links = сообщение с 4 тапабельными кнопками (Админка/Каталог/Лендинг/Health), гейт OWNER_TG_ID; Markup импортирован; адреса по K30 живут в боте, не в памяти.
Health: GET /health при Accept: text/html → человечья страница (крупно «OK / сервер жив / время»), иначе прежний JSON (curl/smoke/бот не затронуты).
Стем-схема (класс-фикс «порядок слов в диктовке», директива владельца 17.09: без мелких заплаток, смотреть широко): agr() products.js = 45 групп стем-наборами (порядок не важен: «воздушный фильтр» = «фильтр воздушный»); KM робота admin.html = единый формат [alts, категория], alt = regex ИЛИ массив AND-стемов («литой диск» = диск+лит); серверный поиск q = токены-AND по article/name/category/car_brand/model (было LIKE целой фразой). K-правило: новые словари свободки = только стем-наборы, строки фиксированного порядка запрещены.
Аналоги (вариант A, выбор владельца 17.09): аналог = пересечение cross_numbers ИЛИ (та же группа agr И та же применимость: car_brand IS ? AND model IS ?, NULL-safe; оба NULL = совпадает). Утечка между моделями исключена (тест 3136/3034 = 0 в обе стороны). Следствие в демо: аналоги часто пусты до заполнения model/cross_numbers — очередь: обогащение демо-строк model по имени или cross_numbers из прайсов, по слову владельца.
Урок «Камри≠Королла»: поиск и аналоги по применимости (поле model), не по совпадению слов; имя и модель карточки согласованы; приёмочные строки писать только из фактов (поля строки), моя строка «Corolla в выдаче» была ошибкой ложной гипотезы.
Операции: docker builder prune -f в конце каждой деплой-цепи после зелёного smoke (build-кэш ~600MB рекреится каждой сборкой; диск 91%→85-86% DISK_OK); страховка паспорта = git-коммит после дельты (K35).
Приёмка зелёная 17.09: node eval 3/3, smoke 4×200 + DISK_OK, /links кнопки открывают, human-health читаем, аналоги 3136/3034 = [] честно, утечки 0/0.
§41.3 ДЕЛЬТА 17.09.2026 (ночь) — обогащение демо, сторона в agr, урок границы слова
Обогащение (слово владельца): из 105 демо-строк с пустой model UPDATE по имени: ровно один алиас модели → каноническая model (SET=57); ноль или несколько → skip с причиной в отчёте (SKIP=48: масла/универсал=0 баз, «Logan/Almera»/«Solaris/Rio»/«2108-2110»=2 базы, ВАЗ без модели=0). Бэкап перед правкой: /root/zapdemo/backups/parts_pre_enrich_20260917.db (§35). Отдача: подбор «марка→модель» и бейдж «Подойдёт» на всём ассортименте, поиск «для камри» находит старые карточки. Пары «одна деталь — несколько машин» остаются без model — очередь: таблица применимости product_models (S-пункт), не заплатка.
agr: сторона L/R для групп te/ar/sh (левый ≠ правый в аналогах); баг «лев» внутри «руЛЕВОЙ» лечится границей слова (^|[^а-яё]). K-правило расширено: словарные матчинги свободки = стем-наборы И границы слова; подстроки без границ запрещены (тот же класс дефекта, что фиксированный порядок слов).
Границы аналогов в демо: при одной детали на (тип×машина) пары по применимости редки; аналоги живут на группах с совпавшей применимостью (масла Universal 3039/3040/3041) и на cross_numbers; приёмка: 3068/3069 = [] (сторона развела), 3039 → масла = 1.
Операции: docker builder prune -f в конце деплой-цепи подтверждён дважды (86% DISK_OK).
Приёмка зелёная: smoke 4×200 + DISK_OK, SET=57 SKIP=48, аналоги []/[]/1.
§41.5 ДЕЛЬТА 18.09.2026 — статус sent_vk, очередь симулятора, шаги к видео
Outreach: владелец дал оффер в VK; бот: статус sent_vk добавлен в ST/SDESC («отправлено в VK»); в /stats VK-касания входят в сумму «Касаний» и конверсию (sent_wa+sent_tg+sent_sms+sent_vk+called); построчная строка sent_vk в /stats автоматом. Приёмка: /status <телефон> sent_vk принят без списка формата.
Очередь симулятора (директивы владельца 17–18.09): гостевой вход в админку = клиентская иллюзия (снимок демо-данных в sessionStorage, мутации применяются локально и отвечают успехом, ноль записей на сервер; баннер честности «Симулятор магазина»; серверная 403-страховка гостевых мутаций; пункт бурга «Для сотрудников»). Параллельные гости независимы по архитектуре (sessionStorage изолирует вкладки); серверная песочница отклонена (реальные записи, диск, джоба сброса, столкновения). read-only отклонён владельцем: гость должен чувствовать полные права (создавать/удалять/красить) без сохранения.
Шаги к видео (порядок зависимостей): 0 факты (login/session/cookie admin.js, схема tenants, мутирующие роуты — отложены владельцем) → 1 симулятор+приёмка → 2 причёсывание демо (cross_numbers 3–5 группам, покрытие фото, брендинг) → 3 раскадровка 30–60 сек (лендинг→гостевой вход→робот→товар в каталоге→смена цвета→удаление→баннер+CTA) → 4 запись владельцем (OBS ПК + мобильный фрагмент, склейка+субтитры) → 5 пост «запусти магазин за 30 секунд без регистрации» + встраивание в лендинг → 6 измерение (?from=social + счётчик гостевых входов).
ProStors: упаковку тиражирует куратор ProStors по наработкам zapdemo (§41.4); HANDOFF.md готовлю по слову «передача».
Рынок (18.09): симулятор админки не эксклюзив в широком SaaS (триалы/песочницы/туры обычны), но редок в нише автозапчастей и в подтипе «без регистрации»; пост строить вокруг 30-сек видео-демо, не вокруг идеи.
Приёмка зелёная: smoke 4×200 + DISK_OK, DEPLOY_DONE, sent_vk принят.
§41.6 ДЕЛЬТА 18.09.2026 — текст А v3 (outreach)
Текст А для первого касания outreach-бота переписан (директива владельца 18.09): убран блок про тарифы в первом касании (тарифы живут на лендинге и в микро-ответах на возражения), первое письмо чище.
Текст А v3 (дефолт в bot.js): «Здравствуйте! Вижу, что вы профессионально занимаетесь продажей запчастей для авто. Мы готовы за один день запустить для вас работающий интернет-магазин в Telegram и вебе. Интеграция с вашей базой и сайтом. Всё настроим под ваш бренд сами: загрузим прайс из Excel, подключим уведомления. Цена: 2490 ₽/мес. Скиньте ваш прайс-лист в ответном сообщении — я настрою и бесплатно покажу демо магазина прямо на ваших деталях!»
Приёмка: /nextt в боте → под «— ТЕКСТ ДЛЯ ОТПРАВКИ —» пять абзацев слово в слово, начинается с «Здравствуйте!», без скобки про тарифы.
Приёмка зелёная: smoke 4×200, DISK_OK 86%.
§41.7 ДЕЛЬТА 18.09.2026 — текст А v3 в бою, фикс дубля хвоста
Текст А v3 (§41.6) в боевой эксплуатации: /nextt выдаёт пять абзацев слово в слово, хвост одиночный (приёмка 12:56). Дефект патча pb5: хвост «магазина прямо на ваших деталях!» задублировался (хвост добавлен явно И пришёл из остатка-среза строки после замены границ); pb6 = одна замена дубля, деплой зелёный.
Урок (расширение K18/K25): при замене фрагмента строки по граничным индексам остаток-срез УЖЕ содержит хвост — не добавлять хвост явно вторым; приёмка текстов = сверка вывода бота слово в слово, не только smoke.
Операции: деплой-цепь с docker builder prune -f в конце (86% DISK_OK), K22 зелёный (prostors/app/ernest=200).
Очередь без изменений: симулятор магазина (ступень 0 — факты login/session/cookie admin.js, схема tenants, мутирующие роуты — за владельцем), таблица применимости product_models для пар «Solaris/Rio» (S-пункт), HANDOFF.md для куратора ProStors по слову «передача», бот-подсказка ближайшего номера по слову «подсказка», далее S1–S5 → N1 → упаковка с живым демо.


§42. ДЕЛЬТА 19.09.2026 — инцидент потери v1.6, восстановление R2-lite, правила K37
Инцидент (близнец, утро 19.09): слепой git checkout -- public/admin.html без status/diff уничтожил неделю некоммиченного кода (v1.6: MODEL_MAP 67/134 + BA ~38, поле модели, A7); страховки обесценены им же: prune 17-18.09 без whitelist последнего хорошего фронта, код не коммичен месяц (b05bea9 24.08). R1: база 11.09 из архива rollback-20260913.tgz + sim2 в прод.
Восстановление (новый куратор): fixpoint 51164e0; restore_parser через ФМ дал два словаря и рассинтакс; мой патч удаления лишней ; схемой s[:idx]+new_js+s[end:] ВЫРЕЗАЛ открывающий тег <script> → извлечения пусты, node --check ложно-зелёный, aiParse is not defined = тесты поверх пустоты. Лечение: docker cp zap_app:/app/public/admin.html (известное хорошее прода) → цепь r2a (MODEL_MAP 38 алиасов/BA 11) → r2c (численные раны) → r2d (перезапись цены/имени, фолбэк для X) → r2f (четыресто:400 в H и NW, полировка имени) → gt-харнес /tmp/mk.py собирает /tmp/gt_one.js = браузер-стабы + ВЕСЬ inline-скрипт + 3 теста, node из образа.
gt 19.09: B зелёный полностью (Подшипник Corolla/3500/1/Toyota/Corolla); A поля зелёные (3400/2/Фильтры/Toyota/Camry) + хвост « лей» в имени (урок §39: \w*→[а-яё]*, в очереди); C цена 35000 зелёная, бренд = фолбэк «для X» по R24 (словарь усечён, Lamborghini в очереди донабора).
Деплой 19.09: build 24.2s → stop/rm/run → nginx -t/reload (П6) → smoke 4×200 (K22) → DISK_OK 86% → builder prune 501.5MB → commit e13d349 + push (хук). Приёмка владельца в новой вкладке проведена с учётом очереди.
Правила K37: K37.1 commit+push кода в конце каждой цепи (хук страхует push); K37.2 git status+diff ДО любого checkout/reset; K37.3 prune только с whitelist-тегом zap_app_image:last_good_frontend (тег ставить до prune); K37.4 gt парсера (node из образа) ДО деплоя, красный = стоп; K37.5 gt-харнес /tmp/mk.py + /tmp/gt_one.js = регрессионный инструмент, в конце тестов process.exit(0) — иначе верхнеуровневый таймер inline-скрипта держит контейнер живым (висячие gt-контейнеры убивать по именам); K37.6 длинные однострочные цепи бьются при paste — деплой тремя короткими блоками (build / recreate+nginx / smoke+commit).
Очередь: R2b (поле «Модель авто» с подписью + проводка editProd/aiFill/payload + genDesc «Применяемость»); хвост « лей» (\w*→[а-яё]*); донабор MODEL_MAP/BA до 67/134+38 (Lamborghini, Vitara); A7 (гамбургер + «Состояние»); product_models (S-пункт); HANDOFF по слову «передача».
<!-- DELTA42 -->


§43. ДЕЛЬТА 19.09.2026 (вечер) — хвост « лей»: порядок альтернатив + границы слова; пункт 1 очереди закрыт
Факты: grep руб\w = 0 и руб[а-яё] = 0 → первая гипотеза (\w* из §39) неверна; истинная причина в строке 312: replace-список (в наличии|штуки|штук|штука|шт|руб|рублей|рубля|₽|цена) — альтернатива руб стоит РАНЬШЕ рублей и без границ слова: на слове «рублей» матчит первые 3 буквы → хвост « лей»; тот же дефект калечил названия деталей с подстрокой «руб» (тРУБка/тРУБа/патРУБок) — кейс владельца.
Прецедент: §41.3 «лев» внутри «руЛЕВОЙ» — тот же класс дефекта (подстрока без границ); K-правило: словарные матчинги = стем И границы слова.
Фикс (fix_lei): строка 312 позиционно (K18: подстрока неуникальна, 3 вхождения): порядок длинное-первое (рублей|рубля|руб) + lookaround (?<![а-яё])…(?![а-яё]) вокруг группы.
gt (харнес /tmp/mk.py + /tmp/gt_one.js, node из образа, process.exit(0) в конце тестов): A «Фильтр масляный для Camry» без хвоста; B/C без изменений; D «трубка кондиционера для камри в наличии две штуки три тысячи четыреста рублей» → имя «Трубка кондиционера для Camry» ЦЕЛОЕ (доказательство защиты номенклатуры), 3400/2/Toyota/Camry.
Деплой: build 24.3s → recreate → nginx -t/reload → smoke 4×200 → DISK_OK 86% → prune 501.5MB → commit 7b73ff7 + push → DEPLOY_DONE; приёмка владельца в новой вкладке.
Урок (расширение §41.3/K-правила): в replace-списках вычитания слов обязательны порядок длинное-первое И границы слова; верный список слов без порядка/границ калечит и хвост цены, и номенклатуру.
Очередь: пункт 1 (хвост) закрыт; остаток: R2b (поле «Модель авто» + проводка + «Применяемость»), донабор MODEL_MAP/BA до 67/134+38 (Lamborghini, Vitara), A7 (гамбургер + «Состояние»), product_models (S-пункт), HANDOFF по слову «передача».
<!-- DELTA43 -->

## §17. СОХРАННОСТЬ, БЭКАПЫ, ЗАЩИТА ОТ ПОТЕРЬ (на 21.09.2026)
17.1 Код и паспорт: git-репо /root/zapdemo → github.com/777ernest888-oss/zapdemo. Каждый деплой заканчивается commit+push. Зелёная точка приёмки = тег last_good_frontend (обновляется после приёмки владельца). Откат — по §18.
17.2 БД parts.db: cron ежедневно 04:00 — sqlite3 ".backup" в /root/zapdemo/backups/parts-YYYYMMDD.db, ротация find -mtime +7.
17.3 Вся data (БД + uploads/фото): cron ежедневно 04:30 — /root/zapdemo/backup_zap.sh → zap-full-YYYYMMDD.tar.gz там же, ротация 7 дней. Ручной прогон: тот же скрипт.
17.4 Восстановление: БД — docker stop zap_app, заменить parts.db, docker start zap_app; полное — распаковать tar в /root/zapdemo/data/.
17.5 НЕ защищено (известный риск): внеплощадочная копия БД/uploads (github — только код и паспорт). Гибель сервера = потеря данных новее последнего ночного бэкапа. Еженедельный вынос на внешний носитель — решение владельца.
17.6 .bak в src/routes и /root/*.tgz — исторические срезы, НЕ система бэкапа; не полагаться, не удалять без владельца.
17.7 После любых правок cron/бэкапов — назавтра проверить ls -lt /root/zapdemo/backups и лог.

## §18. УРОКИ ОТКАТА (инцидент с затиранием кода)
18.1 Инцидент (сентябрь 2026): откат рабочего файла к старому состоянию стёр серию незакоммиченных патчей; восстановление шло заново по gt-кейсам.
18.2 Правила: (1) commit+push ДО рискованных правок; (2) откат только к тегу last_good_frontend и только git checkout <tag> -- <path> после просмотра git diff; (3) запрещён слепой checkout/restore рабочих файлов с незакоммиченными правками; (4) деплой строго тремя блоками (build / recreate+prune+nginx / smoke+commit+push); (5) gt-кейсы парсера A–K перед деплоем; (6) патч только после grep-фактов якорей, assert уникальности якоря; (7) не запускать один патч дважды без проверки состояния (инцидент двойной вставки).

## §19. ПАСПОРТ — ЕДИНСТВЕННЫЙ ИСТОЧНИК ИСТИНЫ
PASSPORT.md — единственный источник истины по проекту ZapDemo. Читать полностью ОБЯЗАТЕЛЬНО до начала любых работ. После каждой принятой правки исполнитель обязан в той же сессии внести дельту в паспорт (что изменено, где, как проверить).

## §20. ОНБОРДИНГ НЕИНФОРМИРОВАННОГО ИСПОЛНИТЕЛЯ
20.1 Прочитать §0–§19 полностью.
20.2 Окружение: хост qktjzelbmz (Ubuntu); контейнер zap_app (образ zap_app_image, сеть prostors-saas_prostors_net, mount /root/zapdemo/data:/app/data, memory 192m); внешний nginx prostors_nginx (zap.prostors.ru → контейнер); БД SQLite data/parts.db, схема src/db/schema.sql + миграции src/db.js; админка public/admin.html (пароль в .env), каталог public/catalog.html; робот-парсер aiParse внутри admin.html; словари — distinct-значения из БД.
20.3 Процесс: grep-факты якорей → python-патч с assert → синтакс-проверка (docker node --check / экстракт script) → gt (/tmp/mk.py + кейсы) → деплой тремя блоками → приёмка владельца → тег last_good_frontend → дельта в паспорт.
20.4 Ключевые запреты: якоря по памяти; патчи без assert; двойной прогон патча; RegExp-строки без двойного экранирования; NBSP не вычистить (sed 's/\xc2\xa0/ /g' по скрипту).
20.5 Где что: бэкапы /root/zapdemo/backups (crontab -l), логи docker logs zap_app, репо github, исторические срезы src/routes/*.bak, gt-кейсы пересоздаются /tmp/mk.py.

<!-- DELTA44 -->
§44. ДЕЛЬТА 21.09.2026 — offsite backup через Telegram-бота (риск §17.5 закрыт)

Решение владельца (вариант б из §33): шифрование AES-256-CBC + доставка документа владельцу командой /backup в @zapdemo777bot.

Реализация:
1. .env: добавлена переменная BACKUP_KEY=<openssl rand -hex 32> (секрет, не в git).
2. backup_zap.sh переписан: tar → openssl enc -aes-256-cbc -pbkdf2 -iter 200000 -salt → rm plaintext → ротация *.tar.gz.enc (mtime+7). Plaintext-архивы удаляются сразу после шифрования.
3. src/bot.js: добавлены require('fs')/require('path'); новая команда bot.command('backup'): auth(c) → readdirSync('/app/backups').filter(.tar.gz.enc).sort() → replyWithDocument(latest) с caption «🔒 Offsite Backup\n<filename>\nКлюч: BACKUP_KEY в .env».
4. Контейнер запускается с дополнительным mount: -v /root/zapdemo/backups:/app/backups:ro (read-only, безопасность от записи ботом).

Проверки: SYNTAX_OK (docker node --check); BUILD_OK; DEPLOY_DONE; ZAP_HEALTH:200; PROSTORS_MAIN:200 (K22); GIT_PUSHED e8d887e; приёмка владельца скриншотом TG (документ получен 12:11, размер 3,3 МБ).

Инструкция восстановления вне сервера: скачать файл из чата бота → openssl enc -d -aes-256-cbc -pbkdf2 -iter 200000 -in <file>.enc -out restored.tar.gz -pass pass:<BACKUP_KEY> → tar -xzf restored.tar.gz -C /root/zapdemo/data/.

Урок K38: перед патчем JS-модуля проверять grep фактических require в шапке файла; предыдущий провал fs is not defined произошёл из-за ложного предположения о наличии импорта. Правило: факты до диагноза распространяются и на наличие модулей.

Текущее состояние: риск потери данных при гибели VPS устранён (еженедельный ручной /backup по запросу владельца; автоматический cron-пуш воскресенье 10:00 — очередь v2).

Команда деплоя: docker build → stop/rm/run (+mount backups ro) → nginx reload → smoke 4×200 → commit/push → тег last_good_frontend.
<!-- /DELTA44 -->

<!-- DELTA45 -->
§45. ДЕЛЬТА 21.09.2026 — донабор словарей парсера (марки/модели автозапчастей РФ)

Решение владельца: расширить покрытие рынка с ~30% до целевых 90-95%, чтобы робот распознавал китайские, премиум и коммерческие марки при диктовке текста.

Реализация:
1. public/admin.html (строки 308-309):
   - var BA расширен с 12 до 30 марок (+Nissan, Mazda, Mitsubishi, Suzuki, Mercedes, BMW, Audi, Peugeot, Citroen, Haval, Chery, Geely, Changan, Exeed, Jetour, Tank, ГАЗ, УАЗ). Алиасы включают латиницу и кириллицу.
   - var MODEL_MAP расширен с 40 до 148 записей (~74 уникальных моделей × 2 алиаса). Добавлены ключевые модели для новых марок (Qashqai, CX-5, Outlander, GLC, X5, Jolion, Tiggo, Coolray, Газель Next, Патриот и др.).
2. Инструмент тестирования: создан изолированный харнес test_parser_v4.py, который вырезает чистую логику парсера из HTML (без DOM/UI мусора), добавляет минимальный стаб window={} и прогоняет GT-кейсы A-K + новые (CN/EU/JP/RU). Это решило проблему бесконечных ошибок ReferenceError/TypeError при попытке эмулировать весь браузер.

Проверки: SYNTAX_OK (docker node --check); GT 10/10 PASS (A,B,C,H,J,NEW-1..5); BUILD_OK; DEPLOY_DONE; ZAP_HEALTH:200; PROSTORS_MAIN:200 (K22); GIT_PUSHED 8ddf176.

Урок K39: диагностика «нет данных» (grep const → пусто) без проверки альтернативных объявлений (var/let) привела к ложному выводу о смене структуры кода. Правило §0.3 («факты до диагноза») распространяется на поиск переменных: проверять все варианты объявления перед постановкой диагноза. Также подтверждено правило §20.4: обязательная очистка NBSP (sed 's/\xc2\xa0/ /g') перед запуском Python-скриптов, полученных через paste.

Текущее состояние: парсер v1.7 готов к боевой эксплуатации. Очередь расширения: Lexus, Infiniti, Volvo, Land Rover/Jaguar, Chevrolet, Fiat, Jeep, Porsche, Mini, Datsun, SsangYong + уточнение моделей Toyota RAV4/Land Cruiser/Hilux, Mazda CX-3/CX-8.

Команда деплоя: стандартная цепь из 3 блоков (§20.3). Тестирование: python3 test_parser_v4.py.
<!-- /DELTA45 -->


## §45 дельта — 22.09: инцидент «кнопка Сохранить» (4 суток), пакет фиксов, backups mount
- Корень мёртвой кнопки: дублирование обёрточной цепочки saveProd (_sp2/_sp3, коммит куратора 34ca77e): перезапись var между копиями -> взаимная рекурсия -> stack overflow в момент клика, тихо (нет catch). Фикс 4eef79f: обёртки удалены целиком; базовый saveProd самодостаточен (msg/clearForm/loadP).
- cf77fe6: валюта целым словом с границей + замена на пробел в обеих копиях базового парсера (склейка «газельлей»); f6c23d8: дедуп лейбл-IIFE (var+forEach целиком), «Состояние» только из явной диктовки, гард f_country в saveProd.
- Канонический docker run zap_app ТЕПЕРЬ включает: -v /root/zapdemo/backups:/app/backups:ro (роут /admin/backups листает /app/backups; без тома ENOENT 500, клиент поллит каждые 3с).
- Правила: дедуп копий = удалять целиком + проверять скоуп осиротевших ссылок; вердикт gt = exit-код раннера со счётчиком PASS; HARNESS_SWALLOWED = красный; висящий gt-контейнер = kill + docker logs; sed = regex (литеральные замены только python str.replace); факты разметки = fold, не cut; серверная правда (curl, access-лог nginx, md5 отдачи vs диска) первичнее клиентских гипотез; nginx access.log = симлинк на /dev/stdout (tail виснет, брать docker logs); в Node-пробе голый fetch != window.fetch, function declaration не попадает в window; NBSP скрабить sed'ом по ВСЕМУ файлу скрипта до запуска python, включая отступы.
